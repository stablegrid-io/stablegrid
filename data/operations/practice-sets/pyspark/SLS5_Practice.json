{
  "topic": "pyspark",
  "title": "Practice Set SLS5 — Reviewing Layout Decisions",
  "description": "Senior storage layout recognition track. Module 5 of 5 — reviewing layout decisions.",
  "version": "authored 2026-05-04 (concise, 17-bias), targets PySpark 3.5+",
  "metadata": {
    "moduleId": "module-SLS5",
    "trackLevel": "senior",
    "estimatedDurationMinutes": 60,
    "maximumDurationMinutes": 90,
    "taskCount": 2,
    "scenarioCompany": "GridUnion Continental",
    "modulePhase": "storage_layout_recognition_senior",
    "taskTypeMix": {
      "refactor_reading": 2
    }
  },
  "datasets": [
    {
      "id": "SLS_meters",
      "file": "SLS_data/meters.csv",
      "description": "Schema reference (sample rows). Senior scenarios describe GridUnion Continental at platform scale (8M+ meters, 6 teams, 4 countries, 30 regions); the CSV is a small sample for schema reference.",
      "schema": {
        "columns": [
          {
            "name": "meter_id",
            "type": "string",
            "nullable": true
          },
          {
            "name": "daily_kw",
            "type": "double",
            "nullable": true
          },
          {
            "name": "date",
            "type": "string",
            "nullable": false
          },
          {
            "name": "region",
            "type": "string",
            "nullable": true
          },
          {
            "name": "customer_type",
            "type": "string",
            "nullable": true
          },
          {
            "name": "substation_id",
            "type": "string",
            "nullable": false
          }
        ]
      },
      "usedByTasks": [
        "module-SLS5-task-01",
        "module-SLS5-task-02"
      ]
    },
    {
      "id": "SLS_substations",
      "file": "SLS_data/substations.csv",
      "description": "Substations reference. Anchors broadcast/partition scenarios.",
      "usedByTasks": [
        "module-SLS5-task-01",
        "module-SLS5-task-02"
      ]
    },
    {
      "id": "SLS_tariffs",
      "file": "SLS_data/tariffs.csv",
      "description": "Per-customer-type tariffs reference.",
      "usedByTasks": [
        "module-SLS5-task-01",
        "module-SLS5-task-02"
      ]
    }
  ],
  "tasks": [
    {
      "id": "module-SLS5-task-01",
      "title": "Reviewing a Layout Proposal",
      "type": "refactor_reading",
      "estimatedMinutes": 28,
      "description": {
        "context": "GridUnion's Reporting team senior submitted a pull request to rebalance silver.aggregations — change partition scheme from (date) to (date, region_code), add ZORDER BY (customer_type, daily_kw), reduce VACUUM retention from 30 days to 7 days. Risk team's architect reviews. The PR's stated goal is to speed up Reporting's 8 daily summary queries. The architect must check four cross-team risk dimensions: upstream contract impact, downstream optimization unlock, migration cost, and rollback path.",
        "task": "Read the artifact above, then answer the questions in order."
      },
      "evidence": [
        {
          "type": "text",
          "title": "Dataset",
          "content": "Cross-team layout-rebalance PR with four review dimensions to evaluate"
        },
        {
          "type": "code_block",
          "label": "Plan output",
          "language": "text",
          "content": "# PR: rebalance-silver-aggregations\n# Author: Reporting team senior\n\n# Changes proposed:\n#   1. ALTER TABLE silver.aggregations PARTITIONED BY (date, region_code)\n#      Was:  partitionBy(date)        — 365 partitions\n#      To:   partitionBy(date, region_code)  — 365 × 4 = 1460 partitions\n#\n#   2. OPTIMIZE silver.aggregations ZORDER BY (customer_type, daily_kw)\n#      Net effect: file-skipping on Reporting's customer_type filter pattern\n#\n#   3. ALTER TABLE silver.aggregations SET TBLPROPERTIES (\n#        'delta.deletedFileRetentionDuration' = 'interval 7 days'\n#      )\n#      Was:  30 days  →  To:  7 days\n\n# Cross-team usage:\n#   Reporting: 8 daily summary queries — primary beneficiary\n#   Risk:      pinned time-travel reads — depends on 30-day retention\n#   Compaction: nightly maintenance — reads partition layout for incremental work\n#   Audit:     30-day rollback for compliance — depends on 30-day retention\n\n# Migration plan in PR:\n#   - Run ALTER TABLE on silver.aggregations in maintenance window\n#   - Re-OPTIMIZE entire table (estimated 8 hours, 4 TB rewrite)\n#   - VACUUM with new 7-day retention immediately after"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "SLS5-01-Q1",
            "type": "single_select",
            "label": "Q1 — Does Change #1 (partition scheme change) break upstream or downstream contracts?",
            "options": [
              "Yes — Compaction's nightly job reads partition layout for incremental processing. Changing from (date) to (date, region_code) means Compaction sees 4× more partitions per night. Coordination required before merge.",
              "No — partition scheme changes are local to the table; they don't affect upstream or downstream consumers in any way. The PR is safe to merge without coordination overhead.",
              "No, but only because (date, region_code) preserves date as the leading partition column. If region_code came first, downstream would break. The current ordering is safe.",
              "Yes — Reporting's queries that filter on date alone will read 4× more partitions because the leading partition column is unchanged. The change actively HARMS Reporting's queries while supposedly helping them."
            ],
            "correctAnswer": "Yes — Compaction's nightly job reads partition layout for incremental processing. Changing from (date) to (date, region_code) means Compaction sees 4× more partitions per night. Coordination required before merge.",
            "rationale": "Compaction's incremental logic reads partition_id from the file system to track 'what's new since last run'. Changing the partition scheme changes the partition identity. After the change, Compaction sees every partition as new on its first post-change run — full reprocessing instead of incremental. Cross-team coordination is required: Compaction needs warning, possibly a one-time full re-base. The PR cannot merge without consulting Compaction. This is a recurring cross-team failure mode in lakehouses; partition scheme changes have downstream contract effects beyond the immediate query layer.",
            "distractorRationale": "Option B: Treats partition changes as local; they affect every consumer of partition_id.\n\nOption C: Treats column ordering as the only concern; identity change matters too.\n\nOption D: Misreads pruning behavior; date-only filters still prune to date directories regardless of inner column order."
          },
          {
            "id": "SLS5-01-Q2",
            "type": "single_select",
            "label": "Q2 — Does Change #2 (ZORDER BY customer_type, daily_kw) interact with Change #1's partition scheme?",
            "options": [
              "Yes, harmfully. Z-order conflicts with multi-column partitioning. The OPTIMIZE rewrite breaks the partition spec; queries fail until manual repartitioning is applied.",
              "Yes, harmfully. Z-order conflicts with multi-column partitioning. The OPTIMIZE rewrite breaks the partition spec; queries fail until manual repartitioning.",
              "Yes, beneficially. With (date, region_code) partitioning, each partition is small. Z-order on customer_type creates fine-grained file-skipping within each partition.",
              "Yes, but only if customer_type is added as a third partition column. Otherwise Z-order on customer_type is a no-op because partition pruning hides it from the file-skipping evaluator."
            ],
            "correctAnswer": "Yes, beneficially. With (date, region_code) partitioning, each partition is small. Z-order on customer_type creates fine-grained file-skipping within each partition.",
            "rationale": "Partitioning and Z-order compose. Partitioning prunes at directory level (which dates × regions to even consider); Z-order skips files within each partition based on (customer_type, daily_kw) min/max stats. With ~3MB per partition (4TB / 1460 partitions ≈ 2.7GB per partition × 1-2 files), each partition ends up with a small number of Z-ordered files where customer_type/daily_kw stats add another layer of skipping. The architecture is the canonical 'partition by primary filter columns, Z-order by secondary filter columns'. This is the right pattern.",
            "distractorRationale": "Option A: Treats them as independent; Z-order's effectiveness depends on partition size.\n\nOption B: Treats Z-order as conflicting with partitioning; they compose.\n\nOption D: Invents a partitioning prerequisite for Z-order; not required."
          },
          {
            "id": "SLS5-01-Q3",
            "type": "single_select",
            "label": "Q3 — Change #3 (VACUUM retention 30→7 days): what is the risk?",
            "options": [
              "No risk — 7 days is reasonable VACUUM retention; storage savings outweigh marginal coordination cost. Most lakehouses run with 7-day retention as the default policy.",
              "Risk's pinned time-travel breaks — needs 30-day retention. Audit's 30-day rollback breaks — regulatory requirement. Reject or split the change: keep retention at 30 days, address Reporting separately.",
              "Risk is moderate — Audit's 30-day rollback fails on day 8 onward, but only triggers if regulators audit during that window (statistically rare). Accept the risk for the storage savings.",
              "Risk is bidirectional — Reporting's own 4-6 hour queries become exposed to stranded-reader incidents if any individual query exceeds 7 days. The PR fix protects against the team that submitted it as well."
            ],
            "correctAnswer": "Risk's pinned time-travel breaks — needs 30-day retention. Audit's 30-day rollback breaks — regulatory requirement. Reject or split the change: keep retention at 30 days, address Reporting separately.",
            "rationale": "30→7 day retention reduction is the most dangerous change in this PR. Risk's compliance time-travel reads break the moment the change applies (older snapshots inaccessible). Audit's 30-day rollback regulatory requirement directly fails — this is a compliance breach, not a performance miss. The architect must REJECT the retention change and require it be split out: the partition scheme + Z-order changes can proceed independently; the retention change requires regulatory review and probably must NOT be made. Cost-asymmetry argument from SLS4.2 applies here: storage cost vs compliance failure is bounded vs unbounded.",
            "distractorRationale": "Option A: Treats the retention change as local; it directly violates Audit's regulatory requirement.\n\nOption C: Trades regulatory risk for marginal storage savings; senior architects don't make this trade.\n\nOption D: Notes a real concern but underweights the compliance failure."
          },
          {
            "id": "SLS5-01-Q4",
            "type": "single_select",
            "label": "Q4 — What is the rollback path if the PR is merged and proves problematic?",
            "options": [
              "Rollback is easy — ALTER TABLE undoes the partition change in seconds; VACUUM retention is reversed by another ALTER TABLE. Only Z-order is irreversible (the rewrite is one-way), but a future OPTIMIZE without ZORDER BY restores the prior layout.",
              "Rollback is impossible — once the partition scheme is changed, the data is physically reorganized. The only path is to recreate the table from bronze sources (8+ hours, all consumers offline). Rollback risk is the dominant review consideration.",
              "Rollback is partial. Partition reverts via ALTER + full rewrite (~8h). Z-order reverts via OPTIMIZE without ZORDER (~8h). VACUUM retention reverts but already-deleted files are gone forever. Total: ~16h plus permanent loss.",
              "Rollback path is the same as forward path — run the inverse PR with reversed parameters. Cost is symmetric, ~8 hours either direction. No special consideration needed for the review process beyond standard rebalance."
            ],
            "correctAnswer": "Rollback is partial. Partition reverts via ALTER + full rewrite (~8h). Z-order reverts via OPTIMIZE without ZORDER (~8h). VACUUM retention reverts but already-deleted files are gone forever. Total: ~16h plus permanent loss.",
            "rationale": "Each change has a different rollback profile. Partition scheme: revert via ALTER TABLE + full table rewrite (8 hours of cluster time). Z-order: revert via OPTIMIZE without ZORDER BY (another 8 hours; the previous Z-order data layout is overwritten). VACUUM retention: ALTER TABLE reverts the property in seconds, but files already deleted by the aggressive 7-day VACUUM are GONE — that part of the rollback is unrecoverable. Total: ~16 hours cluster work for partition+Z-order rollback, plus permanent loss of any history older than 7 days from VACUUM. The rollback cost is asymmetric and the VACUUM piece is irreversible. Senior reviewers must weigh forward-cost vs rollback-difficulty.",
            "distractorRationale": "Option A: Underestimates rewrite cost (8 hours per change) and treats VACUUM deletions as reversible.\n\nOption B: Overstates rollback impossibility; partition changes ARE reversible at cost.\n\nOption D: Treats rollback as symmetric; the VACUUM piece is genuinely irreversible."
          }
        ]
      },
      "pssReference": "PSS3 lessons 5-8 (Layout PR Review Process), PSS9 lesson 6 (Cross-Team Layout Coordination)."
    },
    {
      "id": "module-SLS5-task-02",
      "title": "Quarterly Lakehouse Rebalance Audit",
      "type": "refactor_reading",
      "estimatedMinutes": 32,
      "description": {
        "context": "Final SLS exercise. GridUnion's quarterly lakehouse rebalance plan has 4 proposed changes spanning 4 different shared tables. The architect audits the plan for cross-team safety, ordering dependencies, rollback risk, and which changes need upstream contract amendments. Each sub-question addresses a different aspect of multi-team rebalance coordination.",
        "task": "Read the artifact above, then answer the questions in order."
      },
      "evidence": [
        {
          "type": "text",
          "title": "Dataset",
          "content": "Quarterly multi-pipeline lakehouse rebalance plan with 4 layout changes affecting 6 teams"
        },
        {
          "type": "code_block",
          "label": "Plan output",
          "language": "text",
          "content": "# 2026 Q2 LAKEHOUSE REBALANCE PLAN\n\n# Change 1 — silver.positions: partition scheme MURMUR3 → XXHASH64\n#   Driver: Spark 3.5 default (XXHASH64 is faster)\n#   Effect: every partition_id changes; ~2.4 TB rewrite\n#   Affected: Risk (writer), Compaction (incremental reader), Reporting (some incremental jobs)\n\n# Change 2 — silver.events_clean: Z-ORDER recolumn\n#   From: ZORDER BY (counterparty_id, trade_date)\n#   To:   ZORDER BY (region_code, trade_date)\n#   Driver: Reporting team's query mix shifted; counterparty filters dropped to 5%\n#   Effect: 1.8 TB rewrite\n#   Affected: Reporting (beneficiary), Risk (regression risk)\n\n# Change 3 — silver.regional_dim: VACUUM retention 30 → 90 days\n#   Driver: Audit team's compliance requirements expanded\n#   Effect: ~6× storage growth for tombstoned files (~80 GB more)\n#   Affected: Audit (beneficiary), Operations (storage quota)\n\n# Change 4 — silver.aggregations: bucket count 50 → 200\n#   Driver: customer_dim's bucket-aware joins are 4× the original scale\n#   Effect: full table rewrite (~3 TB)\n#   Affected: 4 teams that join against aggregations; bucket-count must align\n\n# Architectural review checkpoint: cross-team safety + ordering + rollback + contracts"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "SLS5-02-Q1",
            "type": "single_select",
            "label": "Q1 — Change #1 (MURMUR3 → XXHASH64): cross-team impact and required coordination?",
            "options": [
              "Change #1 is a downstream-contract violation. Compaction's incremental processing reads partition_id; XXHASH64 produces different ids than MURMUR3. Compaction reprocesses 100% on first post-change run. Coordinate before the window.",
              "Change #1 is benign; Spark handles hash function transitions transparently. Compaction reads from filesystem, not partition_id; the rename is invisible to consumers. Proceed without coordination.",
              "Change #1 must be deferred until Spark 4.0 release; XXHASH64 has known incompatibilities with Spark 3.5's bucketing logic. The rebalance plan's stated driver is incorrect for the current cluster version.",
              "Change #1 fully deprecates the Spark 3.4 cluster path; both writers and readers must upgrade to 3.5 simultaneously. Coordinate the cluster-wide upgrade as a separate ticket; Change #1 itself is a no-op until that completes."
            ],
            "correctAnswer": "Change #1 is a downstream-contract violation. Compaction's incremental processing reads partition_id; XXHASH64 produces different ids than MURMUR3. Compaction reprocesses 100% on first post-change run. Coordinate before the window.",
            "rationale": "This is the same partition-id contract violation pattern from PRS5.2 Q1. Compaction's incremental skip logic implicitly contracted on partition_id stability. The hash function rename changes every partition_id silently, breaking that contract. Either side can fix it but coordination is required BEFORE the change applies. The lesson: implicit downstream contracts on plan or layout internals are everywhere in mature lakehouses; rebalance audits MUST surface them. Long-term: replace implicit contracts with explicit ones (modification timestamps, schema-versioned partition identities).",
            "distractorRationale": "Option B: Treats hash rename as transparent; partition_id is the actual contract surface.\n\nOption C: Invents a Spark 3.5 incompatibility not in the scenario.\n\nOption D: Treats it as a cluster-upgrade dependency; the change is at the table layout layer."
          },
          {
            "id": "SLS5-02-Q2",
            "type": "single_select",
            "label": "Q2 — Change #2 (Z-ORDER recolumn): is the rationale sound, and what is the rollback risk?",
            "options": [
              "Rationale is sound — query mix shifted from counterparty_id to region_code. Rollback risk: 1.8 TB rewrite is one-way; if Risk regresses, rollback is another 1.8 TB rewrite. Track Risk's p99 for 2 weeks.",
              "Rationale is unsound — Z-order on a new column is rarely worth the rewrite. Risk's 5% remaining counterparty queries will degrade severely after the change. Reject Change #2.",
              "Rationale is sound but the recolumn should add region_code WITHOUT removing counterparty_id: ZORDER BY (region_code, trade_date, counterparty_id). Three-column Z-order maintains both query patterns.",
              "Z-order changes are immune to rollback risk because OPTIMIZE preserves the prior layout in the transaction log. Risk can time-travel to the pre-OPTIMIZE state if their queries regress."
            ],
            "correctAnswer": "Rationale is sound — query mix shifted from counterparty_id to region_code. Rollback risk: 1.8 TB rewrite is one-way; if Risk regresses, rollback is another 1.8 TB rewrite. Track Risk's p99 for 2 weeks.",
            "rationale": "Z-order recolumn rationale is sound when query mix shifts. The trade-off framework: Reporting gains, Risk's residual 5% may regress, rewrite cost is borne once. Rollback risk is real but bounded — another OPTIMIZE without the new ZORDER BY restores. The senior protocol: change forward, monitor regression for 2 weeks, rollback if Risk's p99 degrades by more than X%. Three-column Z-order (option C) is appealing but Z-order's effectiveness drops sharply past 2 columns; Delta's recommendation is up to 4 columns but with diminishing returns. The architect's call is forward-with-monitoring, not perpetual hedge.",
            "distractorRationale": "Option B: Treats Z-order recolumn as universally bad; it's the right response to query mix shifts.\n\nOption C: Three-column Z-order has diminishing returns and increases OPTIMIZE cost.\n\nOption D: Time-travel doesn't restore physical layout; it restores logical state."
          },
          {
            "id": "SLS5-02-Q3",
            "type": "single_select",
            "label": "Q3 — Change #3 (VACUUM 30→90 days, ~80GB storage growth): the asymmetry calculus from SLS4.2 applies — what is the call?",
            "options": [
              "Defer to Operations team — Operations owns the storage budget and the call is theirs. Audit team works with Operations to find a solution; the architect doesn't have authority to approve the change.",
              "Reject. Storage cost compounds — every regulator-driven retention extension grows storage further. Set a precedent: regulatory requirements must be met by specialized snapshot tables, not by extending shared-table retention.",
              "Approve conditionally — extend retention only on compliance-relevant partitions, not the full table. Use ALTER TABLE silver.regional_dim PARTITION (compliance=true) SET TBLPROPERTIES ('delta.deletedFileRetentionDuration'='90 days').",
              "Approve. 80GB at modern cloud rates is ~$2/month. Audit's expanded compliance requirements are unbounded business risk. Bounded small cost vs unbounded risk: trivial decision in favor of compliance."
            ],
            "correctAnswer": "Approve. 80GB at modern cloud rates is ~$2/month. Audit's expanded compliance requirements are unbounded business risk. Bounded small cost vs unbounded risk: trivial decision in favor of compliance.",
            "rationale": "Direct application of the SLS4.2 cost-asymmetry framework. 80GB storage at cloud rates is trivial; Audit's compliance failure is unbounded. The senior call is approve, not deny. Senior architects do not require Audit to build a separate snapshot infrastructure when extending VACUUM retention costs $2/month. Set the precedent: when storage cost is bounded and small, regulatory requirements win automatically. Confirm Operations has quota headroom; if not, the architectural conversation is about expanding the quota, not denying compliance.",
            "distractorRationale": "Option A: Defers architectural authority; the architect signs off on cross-team trade-offs.\n\nOption B: Treats precedent as more important than the actual cost asymmetry.\n\nOption C: Adds operational complexity (per-partition retention) for marginal savings."
          },
          {
            "id": "SLS5-02-Q4",
            "type": "single_select",
            "label": "Q4 — Change #4 (bucket count 50→200): cross-team alignment requirement?",
            "options": [
              "Alignment is one-directional — only consumers writing to silver.aggregations need to update; consumers reading are unaffected because read-time bucket-skip works on either side independently.",
              "No alignment needed — Spark auto-rehashes bucket counts at query time when joining tables with mismatched bucket counts. The 4 consuming teams are unaffected by the change.",
              "Critical alignment — the 4 teams that join silver.aggregations must ALSO change their bucket counts to 200. Otherwise SLS2.2's bucket-mismatch silent regression hits all 4 teams.",
              "Bucket count change is purely metadata; the underlying parquet files don't move. Spark uses bucket spec metadata for runtime decisions but file layout is stable. The plan's effect estimate is wrong."
            ],
            "correctAnswer": "Critical alignment — the 4 teams that join silver.aggregations must ALSO change their bucket counts to 200. Otherwise SLS2.2's bucket-mismatch silent regression hits all 4 teams.",
            "rationale": "Bucket count alignment from SLI2.2 — both sides of any bucketed join must have matching bucket counts or Spark silently disables bucket-skip. Changing aggregations from 50 → 200 buckets means every team that joins against it on the bucketed column must also rewrite their tables to 200 buckets. Without this propagation, the 4 consuming teams silently regress to full shuffles. The architectural rule: bucket-count changes are cross-team coordination events, not per-table changes. The maintenance window must include all 4 consumer rewrites; ordering is fixed (aggregations first, consumers within 24 hours).",
            "distractorRationale": "Option A: Treats read-side as immune; reads with mismatched buckets shuffle silently.\n\nOption B: Invents auto-rehashing; bucket counts must match exactly.\n\nOption D: Treats bucket changes as metadata-only; the parquet files DO move during bucket count change."
          },
          {
            "id": "SLS5-02-Q5",
            "type": "single_select",
            "label": "Q5 — Looking at all four changes, what is the right ORDERING and which need upstream contract amendments?",
            "options": [
              "All four can run in parallel during the same maintenance window; cluster has capacity for the load. Save 50% of total time by parallelizing all changes together.",
              "Sequential start with retention. Then Z-order. Then bucket alignment. Then hash function last. Contracts: hash function needs Compaction amendment; bucket count needs all 4 consumer amendments.",
              "Reverse order — start with the largest rewrite (#1 at 2.4 TB) to fail-fast if cluster issues arise. Smaller changes are quick recovery if anything goes wrong early.",
              "Order doesn't matter; ordering is a project-management concern, not architectural. The architect signs off on each independently and Operations schedules them."
            ],
            "correctAnswer": "Sequential start with retention. Then Z-order. Then bucket alignment. Then hash function last. Contracts: hash function needs Compaction amendment; bucket count needs all 4 consumer amendments.",
            "rationale": "Sequential ordering is required because each change has different prerequisite contracts. #3 (retention) is fastest and lowest-risk — start there. #2 (Z-order) is contained to one table — second. #4 (bucket count) requires all 4 consumer teams to also rewrite — third, with consumer coordination. #1 (hash function) is most invasive because Compaction's incremental contract is broken — last, after Compaction has migrated their incremental logic. Contract amendments needed: #1 needs Compaction's incremental-contract amendment (modification-timestamp-based skip instead of partition_id), #4 needs all 4 consumer teams' bucket-spec amendments. The architect's audit MUST surface both contract amendments before scheduling.",
            "distractorRationale": "Option A: Parallel execution misses cross-change dependencies.\n\nOption C: Fail-fast strategy makes sense for compute jobs, not for sequential layout changes with dependencies.\n\nOption D: Treats ordering as project-management; it's architectural because of inter-change dependencies."
          }
        ]
      },
      "pssReference": "PSS3 lessons 5-9 (Quarterly Rebalance Audit Process), PSS9 lessons 6-9 (Multi-Team Layout Coordination)."
    }
  ]
}