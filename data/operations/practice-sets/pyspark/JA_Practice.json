{
  "topic": "pyspark",
  "title": "Aggregations Recognition Track — Junior",
  "description": "A 24-exercise recognition track that builds PySpark aggregation reading fluency. Predict outputs, spot the difference between snippets, find bugs that run without errors, and review production-style pipelines. Six modules: JA1 Output Prediction · JA2 groupBy Mechanics · JA3 Filter Order · JA4 Nulls · JA5 Distinct & Approximations · JA6 Reading Real Code. Companion to PS6 (the writing-based aggregations track).",
  "version": "authored 2026-05-01, targets PySpark 3.5+",
  "metadata": {
    "moduleId": "module-JA",
    "trackLevel": "junior",
    "estimatedDurationMinutes": 280,
    "maximumDurationMinutes": 360,
    "taskCount": 24,
    "scenarioCompany": "NordGrid Energy",
    "modulePhase": "aggregations_recognition",
    "taskTypeMix": {
      "outputPrediction": 7,
      "findTheBug": 7,
      "spotTheDifference": 6,
      "refactorReading": 4
    }
  },
  "datasets": [
    {
      "id": "JA_meters",
      "file": "JA_data/meters.csv",
      "description": "Shared NordGrid meter readings (20 rows). Engineered nulls and distinct-value counts so every output-prediction task has a single deterministic answer.",
      "usedByTasks": [
        "module-JA1-task-01",
        "module-JA1-task-02",
        "module-JA1-task-03",
        "module-JA1-task-04",
        "module-JA2-task-01",
        "module-JA2-task-02",
        "module-JA2-task-03",
        "module-JA2-task-04",
        "module-JA3-task-01",
        "module-JA3-task-02",
        "module-JA3-task-03",
        "module-JA3-task-04",
        "module-JA4-task-01",
        "module-JA4-task-02",
        "module-JA4-task-03",
        "module-JA4-task-04",
        "module-JA5-task-01",
        "module-JA5-task-02",
        "module-JA5-task-03",
        "module-JA5-task-04",
        "module-JA6-task-01",
        "module-JA6-task-02",
        "module-JA6-task-03",
        "module-JA6-task-04"
      ]
    },
    {
      "id": "JA_substations",
      "file": "JA_data/substations.csv",
      "description": "Substation reference table (6 rows). Used in JA6 refactor-reading scenarios.",
      "usedByTasks": [
        "module-JA6-task-01"
      ]
    },
    {
      "id": "JA_tariffs",
      "file": "JA_data/tariffs.csv",
      "description": "Per-customer-type tariffs (4 rows). Used in JA6 refactor-reading scenarios.",
      "usedByTasks": [
        "module-JA6-task-02"
      ]
    }
  ],
  "tasks": [
    {
      "id": "module-JA1-task-01",
      "title": "What .agg() Actually Returns",
      "type": "output_prediction",
      "estimatedMinutes": 8,
      "description": {
        "context": "Every PySpark transformation returns a DataFrame, including `.agg()` called directly on a DataFrame without `.groupBy()`. The result is a single-row, single-column DataFrame, NOT a Python number. The column is named after the expression that produced it — for `sum('daily_kw')`, the column name is literally `sum(daily_kw)`. To get a Python float you must follow up with an ACTION like `.collect()[0][0]` or `.first()[0]`. Until an action runs, the variable holds a lazy plan, not a value.",
        "task": "What does the variable `total` hold after this code runs?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 NordGrid meter rows; sum of non-null daily_kw = 39178.40."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum\n\ndf = spark.read.csv('datasets/meters.csv', header=True, inferSchema=True)\ntotal = df.agg(sum('daily_kw'))"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA1-01-F1",
            "type": "single_select",
            "label": "What does the variable `total` hold after this code runs?",
            "options": [
              "A Python float 39178.4 — `.agg()` is an action that immediately returns the aggregated value because the engineer called sum(); arithmetic aggregates eagerly evaluate.",
              "A single-element tuple (39178.4,) — PySpark wraps single-aggregate results in a tuple to mirror pandas' behaviour for `Series.agg()` on a single function.",
              "A DataFrame with one row and one column named `sum(daily_kw)` — `.agg()` is a transformation that returns a DataFrame; the column is named after the expression, not aliased.",
              "An empty DataFrame — `.agg()` requires `.groupBy()` first, so without grouping Spark returns an empty schema and the engineer must add a groupBy to get any rows."
            ],
            "correctAnswer": "A DataFrame with one row and one column named `sum(daily_kw)` — `.agg()` is a transformation that returns a DataFrame; the column is named after the expression, not aliased.",
            "rationale": "Every PySpark transformation returns a DataFrame, including `.agg()` without grouping — the result is a single-row DataFrame whose column is named after the expression itself (`sum(daily_kw)`). To get a Python float you need an action like `.collect()[0][0]` or `.first()[0]`. The most common Junior confusion is treating `.agg()` as eager because it 'looks like' a final computation; in PySpark, only actions trigger execution.",
            "distractorRationale": "Option A: Treats `.agg()` as an action returning a scalar; in PySpark all transformations return DataFrames and only actions like `.collect()` produce Python values.\n\nOption B: Imports pandas intuition where `Series.agg()` on a scalar function returns a scalar; PySpark's API is uniform — DataFrame in, DataFrame out.\n\nOption D: Believes `.agg()` requires `.groupBy()`; in fact `.agg()` on a DataFrame without grouping aggregates over all rows and produces exactly one row.",
            "conceptsTested": [
              "lazy evaluation",
              "DataFrame transformations",
              "aggregate column naming"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA1-task-02",
      "title": "groupBy Output Schema",
      "type": "output_prediction",
      "estimatedMinutes": 9,
      "description": {
        "context": "After `groupBy(col).agg(expr1, expr2, ...)`, the output schema is shaped by a fixed rule: the grouping columns come first, then ONE column per aggregate expression — and PySpark names each aggregate column after the expression that produced it (`sum(daily_kw)`, `avg(daily_kw)`). The original `daily_kw` column is GONE — collapsed into the aggregates — unless you preserved it explicitly. This is why production code aliases every aggregate.",
        "task": "What columns does `printSchema()` print?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows. region values: NORTH, SOUTH, EAST, WEST plus 2 null. Two aggregates inside one `.agg()`."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, avg\n\nresult = df.groupBy('region').agg(sum('daily_kw'), avg('daily_kw'))\nresult.printSchema()"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA1-02-F1",
            "type": "single_select",
            "label": "What columns does `printSchema()` print?",
            "options": [
              "Two columns: region + daily_kw — the two aggregates collapse into one output column because they both operate on daily_kw, and PySpark deduplicates aggregate columns sharing the same source.",
              "Three columns: region + sum(daily_kw) + avg(daily_kw) — the grouping column first, then one column per aggregate expression, named after the expression itself with no automatic aliasing.",
              "Four columns: region + daily_kw + sum(daily_kw) + avg(daily_kw) — the original daily_kw survives the aggregation alongside the two derived aggregate columns for downstream reference.",
              "Two columns: region + aggregate — PySpark assigns a generic 'aggregate' name when multiple aggregates are passed to `.agg()` without explicit aliases."
            ],
            "correctAnswer": "Three columns: region + sum(daily_kw) + avg(daily_kw) — the grouping column first, then one column per aggregate expression, named after the expression itself with no automatic aliasing.",
            "rationale": "After `groupBy('region').agg(...)`, PySpark produces one column per aggregate expression named after the expression, plus the grouping columns first. The original daily_kw is consumed by the aggregates and does NOT survive. Row count is one per distinct grouping value INCLUDING null as its own group (5 rows here). The most common Junior confusion is expecting the source column to remain alongside its aggregate — it does not, which is why `.alias('total_kw')` is the production pattern.",
            "distractorRationale": "Option A: Treats two aggregates as collapsing into one column; each aggregate expression inside `.agg()` produces its own output column independently.\n\nOption C: Believes the original daily_kw survives aggregation; it is consumed by sum/avg and is no longer in the schema.\n\nOption D: Expects PySpark to invent a generic name like 'aggregate'; PySpark always names aggregate columns after the expression itself, which is why aliases exist.",
            "conceptsTested": [
              "aggregate column naming",
              "groupBy schema",
              "null as a group key"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA1-task-03",
      "title": "The Missing Alias",
      "type": "output_prediction",
      "estimatedMinutes": 7,
      "description": {
        "context": "After `.agg(sum('daily_kw'))`, the output column is named `sum(daily_kw)` — PySpark uses the full expression as the column name. The original `daily_kw` is gone, so any downstream reference to `col('daily_kw')` fails at PLAN-RESOLUTION time, BEFORE any data is read. Spark raises `AnalysisException`. The production fix is to alias every aggregate: `sum('daily_kw').alias('total_kw')`, then filter on `total_kw`.",
        "task": "What happens when this snippet runs?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Aggregate has no `.alias(...)`; the `.filter` references the pre-aggregation column name."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nresult = (df.groupBy('region')\n           .agg(sum('daily_kw'))\n           .filter(col('daily_kw') > 5000))\nresult.show()"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA1-03-F1",
            "type": "single_select",
            "label": "What happens when this snippet runs?",
            "options": [
              "Runs successfully — the original `daily_kw` column is preserved through the aggregation, so the filter resolves and only regions whose daily_kw exceeds 5000 are kept.",
              "Runs successfully — PySpark renames the aggregate to just `sum`, but it still resolves any reference to `daily_kw` because Spark tracks source-column lineage internally.",
              "Raises AnalysisException at plan resolution — after `.agg(sum('daily_kw'))` the column is named `sum(daily_kw)`, not `daily_kw`, so `col('daily_kw')` cannot be resolved against the post-aggregation schema.",
              "Runs but returns an empty DataFrame — Spark silently treats the missing column as null, all `null > 5000` comparisons evaluate to false, and every row is filtered out."
            ],
            "correctAnswer": "Raises AnalysisException at plan resolution — after `.agg(sum('daily_kw'))` the column is named `sum(daily_kw)`, not `daily_kw`, so `col('daily_kw')` cannot be resolved against the post-aggregation schema.",
            "rationale": "PySpark names aggregate columns after the full expression — `sum(daily_kw)` — and the original `daily_kw` is consumed. The `.filter(col('daily_kw') > 5000)` refers to a column that does not exist in the post-aggregation schema, so Spark raises `AnalysisException` at plan-resolution time, before reading any data. The fix is to alias: `.agg(sum('daily_kw').alias('total_kw'))` and filter on `total_kw`. The most common Junior confusion is expecting the source column to remain available downstream; it never does after a non-grouped aggregation.",
            "distractorRationale": "Option A: Believes the source column survives aggregation; it is consumed by the aggregate and is not in the output schema.\n\nOption B: Expects PySpark to invent a short name like `sum`; PySpark always uses the full function-of-column expression unless aliased.\n\nOption D: Believes Spark silently produces empty results when a column is missing; Spark fails fast at the plan stage with `AnalysisException`.",
            "conceptsTested": [
              "AnalysisException at plan stage",
              "post-aggregation schema",
              "alias necessity"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA1-task-04",
      "title": "The Downstream Join That Breaks",
      "type": "find_the_bug",
      "estimatedMinutes": 10,
      "description": {
        "context": "When `.agg()` runs without an explicit `.alias()`, the produced column carries the expression as its name (`sum(daily_kw)`). Downstream consumers — joins, filters, downstream withColumn calls — that expect a friendly name like `total_kw` will silently disagree with the actual schema. With `join(on=[col_a, col_b])`, Spark requires those columns to exist on BOTH sides; if a join column exists only on one side, Spark resolves it against whichever DataFrame has it and treats the missing side as null. The result: zero matches and an empty join.",
        "task": "Which line contains the bug, and what is the underlying mechanism?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Pipeline runs cleanly. `.show()` prints the schema header and zero rows. Both sides verified non-empty in isolation."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\n# Side 1: regional totals from meter readings\ntotals = df.groupBy('region').agg(sum('daily_kw'))\n\n# Side 2: regional metadata from a CSV\nregional_meta = spark.read.csv(\n    'datasets/regional_metadata.csv', header=True, inferSchema=True\n)\n\n# Join the two — output should have one row per region with totals + metadata\njoined = totals.join(regional_meta, on=['region', 'total_kw'], how='inner')\n\njoined.show()  # Prints zero rows. Why?"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA1-04-F1",
            "type": "single_select",
            "label": "Which line contains the bug, and what is the underlying mechanism?",
            "options": [
              "Line 4: `.agg(sum('daily_kw'))` is missing `.alias('total_kw')`, so Spark silently swallows an AnalysisException at the join site and returns an empty DataFrame instead of erroring.",
              "Line 12: the join references `total_kw` but `totals` actually has `sum(daily_kw)`; with `on=[list]` the missing-on-one-side column is treated as null on the totals side, so no row matches and the result is empty.",
              "Line 12: `how='inner'` is the wrong join type — it should be `how='left'` to preserve the totals rows even when the metadata side has no match for them.",
              "Line 7: the regional_metadata.csv file is empty when the pipeline reads it; the join produces zero rows because there is no metadata to join against."
            ],
            "correctAnswer": "Line 12: the join references `total_kw` but `totals` actually has `sum(daily_kw)`; with `on=[list]` the missing-on-one-side column is treated as null on the totals side, so no row matches and the result is empty.",
            "rationale": "The visible symptom is on line 12, but the root cause is the missing alias on line 4. After `.agg(sum('daily_kw'))`, totals has columns `region` and `sum(daily_kw)` — NOT `total_kw`. With `on=['region', 'total_kw']`, Spark looks for `total_kw` on both sides. Finding it only in `regional_meta`, it treats the totals side as if every row has `total_kw = null`, and `null = anything` is never true — so zero rows match. The most common Junior confusion is expecting Spark to error loudly; with list-form `on=`, it joins to nothing instead.",
            "distractorRationale": "Option A: Correctly spots the missing alias but invents 'silently swallowed exceptions'; Spark does not swallow AnalysisException, the join runs to completion and returns zero rows.\n\nOption C: Diagnoses the join type. The join type only matters when matches exist; here zero rows match, so changing inner to left would still produce zero matched rows (just preserving unmatched left rows with nulls).\n\nOption D: Blames the input data; the scenario explicitly states both sides have rows when checked independently.",
            "conceptsTested": [
              "aggregate column naming",
              "join key resolution",
              "downstream consequences of missing alias"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA2-task-01",
      "title": "Multi-Column groupBy Row Count",
      "type": "output_prediction",
      "estimatedMinutes": 9,
      "description": {
        "context": "Multi-column `groupBy` produces one row per DISTINCT combination of the grouping columns that ACTUALLY APPEARS in the data — not the Cartesian product of cardinalities, and not the source row count. PySpark also treats `null` as a valid grouping value, so combinations where a grouping column is null still get their own group rather than being dropped. To predict the row count, count the distinct combinations present (including nulls), not the theoretical cross-product.",
        "task": "What number does `print()` output?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows. region: NORTH/SOUTH/EAST/WEST + 2 null rows. customer_type: RESIDENTIAL/COMMERCIAL/INDUSTRIAL + 3 null rows. Distinct (region, customer_type) pairs ACTUALLY present in the data: 14."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum\n\nresult = df.groupBy('region', 'customer_type').agg(sum('daily_kw'))\nprint(result.count())"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA2-01-F1",
            "type": "single_select",
            "label": "What number does `print()` output?",
            "options": [
              "20 — groupBy preserves every input row and just attaches a sum column to each, so the result has the same row count as the source DataFrame.",
              "12 — Cartesian product of grouping-column cardinalities, 4 regions × 3 customer_types, gives 12 possible combinations and groupBy emits all of them.",
              "14 — one row per distinct (region, customer_type) combination ACTUALLY present in the data, including pairs where either column is null (treated as a group like any other).",
              "9 — the count of non-null distinct combinations only; PySpark follows SQL's GROUP BY convention and drops any group whose key contains a null."
            ],
            "correctAnswer": "14 — one row per distinct (region, customer_type) combination ACTUALLY present in the data, including pairs where either column is null (treated as a group like any other).",
            "rationale": "Multi-column groupBy emits one row per distinct combination that appears in the actual data — not the Cartesian product (12), not the source row count (20). PySpark treats null as a normal grouping value, so combinations involving null still produce groups. In this dataset 14 distinct (region, customer_type) combinations are present (including 4 involving null), so the result has 14 rows. The most common Junior confusion is reasoning from cardinalities (4 × 3 = 12) instead of from the data — real datasets are sparse, and only combinations ACTUALLY present become groups.",
            "distractorRationale": "Option A: Treats groupBy as if it preserved input rows; groupBy collapses rows that share the grouping key into one row per group.\n\nOption B: Computes the theoretical Cartesian product; only combinations actually appearing in the data become rows.\n\nOption D: Imports a SQL convention that excludes null groups; PySpark groups null as its own group like any other value.",
            "conceptsTested": [
              "multi-column groupBy",
              "null as a grouping value",
              "row-count prediction"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA2-task-02",
      "title": "Conditional Sum vs Pre-Filter",
      "type": "spot_the_difference",
      "estimatedMinutes": 11,
      "description": {
        "context": "PySpark has two ways to sum 'only NORTH rows': filter the DataFrame first, OR use a conditional expression inside `sum(when(...))`. Both produce the same number, but the work is different. `.filter()` is a NARROW transformation applied during the file scan — non-matching rows never reach the aggregation. `sum(when(...).otherwise(0))` evaluates the expression on every row of the input, contributing zero for non-NORTH rows but still touching them. Catalyst does NOT rewrite one form into the other because the semantics differ subtly (e.g., what `otherwise(0)` does to nulls).",
        "task": "Which snippet is faster on the full 500K-row dataset, and why?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Production: 500K rows; NORTH ~25% of them. Both snippets produce identical output."
        },
        {
          "type": "code_block",
          "label": "Snippet A — conditional inside the aggregate",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, when, col\n\nresult_a = df.agg(\n    sum(when(col('region') == 'NORTH', col('daily_kw')).otherwise(0))\n)"
        },
        {
          "type": "code_block",
          "label": "Snippet B — filter first, then aggregate",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nresult_b = (df\n    .filter(col('region') == 'NORTH')\n    .agg(sum('daily_kw')))"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA2-02-F1",
            "type": "single_select",
            "label": "Which snippet is faster on the full 500K-row dataset, and why?",
            "options": [
              "Snippet A is faster — keeping the conditional inside the aggregate avoids a separate filter pass; Spark fuses the predicate into the aggregate evaluation in one efficient scan.",
              "Snippet B is faster — `.filter()` is narrow and applied during the file scan, so only ~25% of rows reach the aggregator; Snippet A evaluates the conditional on every one of the 500K rows.",
              "Both are equally fast — Catalyst recognises that `sum(when(...))` is semantically equivalent to filter+sum and rewrites Snippet A into Snippet B's plan during optimization.",
              "Snippet A is faster because filter triggers a shuffle to redistribute non-matching rows; Snippet A keeps the data in place and avoids the network cost entirely."
            ],
            "correctAnswer": "Snippet B is faster — `.filter()` is narrow and applied during the file scan, so only ~25% of rows reach the aggregator; Snippet A evaluates the conditional on every one of the 500K rows.",
            "rationale": "`.filter()` is a narrow transformation pushed into the scan, so non-NORTH rows are dropped before any aggregation work happens — Snippet B operates on ~125K rows. Snippet A evaluates `when(...).otherwise(0)` on every one of the 500K rows and then sums them all (with zeros from non-NORTH rows). Outputs are identical, work is not. The most common Junior confusion is trusting Catalyst to rewrite `sum(when(...))` into filter+sum; Catalyst preserves the engineer's chosen expression because the semantics around nulls and non-numeric types are not interchangeable.",
            "distractorRationale": "Option A: Treats `.filter()` as a separate pass; in Spark, filter is narrow and fused into the scan that the aggregation already needs.\n\nOption C: Trusts Catalyst to unify semantically distinct expressions; Catalyst preserves the engineer's choice because `sum(when(...))` and filter+sum differ in null handling.\n\nOption D: Confuses `.filter()` (narrow, no shuffle) with `.groupBy()` (wide); filter never shuffles and `.agg()` without grouping does not shuffle either.",
            "conceptsTested": [
              "narrow vs wide transformations",
              "predicate placement",
              "Catalyst's actual scope"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA2-task-03",
      "title": "Two .agg() Calls vs One",
      "type": "spot_the_difference",
      "estimatedMinutes": 10,
      "description": {
        "context": "Stacking multiple aggregates inside a single `.agg(...)` call computes all of them in ONE shuffle pass. Splitting them across separate `.groupBy(...).agg(...)` calls and then joining the results triggers MULTIPLE shuffles — one per groupBy plus a third shuffle (or broadcast) for the join. Catalyst optimizes within a single logical plan but does NOT detect that two separately-built DataFrames share the same groupBy operation; they appear as independent subtrees.",
        "task": "Which snippet is faster, and what mechanism causes the difference?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Both snippets produce identical (region, total_kw, avg_kw) output. Question is shuffle count."
        },
        {
          "type": "code_block",
          "label": "Snippet A — two aggregations joined back together",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, avg\n\nsums = df.groupBy('region').agg(sum('daily_kw').alias('total_kw'))\navgs = df.groupBy('region').agg(avg('daily_kw').alias('avg_kw'))\nresult_a = sums.join(avgs, on='region', how='inner')"
        },
        {
          "type": "code_block",
          "label": "Snippet B — both aggregates in a single .agg()",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, avg\n\nresult_b = df.groupBy('region').agg(\n    sum('daily_kw').alias('total_kw'),\n    avg('daily_kw').alias('avg_kw')\n)"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA2-03-F1",
            "type": "single_select",
            "label": "Which snippet is faster, and what mechanism causes the difference?",
            "options": [
              "Snippet A is faster — splitting sum and avg into two separate jobs gives Spark more parallelism because the two aggregations run on independent executor groups in parallel.",
              "Both are equivalent in cost — Catalyst detects that the two groupBy calls operate on the same DataFrame and merges them into a single plan, so the physical plans become identical.",
              "Snippet B is faster — stacked aggregates share ONE shuffle by region. Snippet A shuffles twice (one per groupBy) plus a third operation for the join, multiplying shuffle cost.",
              "Snippet B is faster only on small data; on large data the join in Snippet A becomes a broadcast hash join and removes the duplicated shuffle, making both snippets equivalent at scale."
            ],
            "correctAnswer": "Snippet B is faster — stacked aggregates share ONE shuffle by region. Snippet A shuffles twice (one per groupBy) plus a third operation for the join, multiplying shuffle cost.",
            "rationale": "Snippet B triggers exactly one shuffle by region; both aggregates compute in that one pass. Snippet A shuffles three times: once per groupBy, plus a third shuffle (or broadcast) to join the two results. Catalyst does NOT detect that two separately-constructed DataFrames perform the same groupBy — they appear as independent subtrees in the plan. The most common Junior confusion is trusting Catalyst to merge cross-subtree operations; its optimization scope is within a single logical plan, not across different DataFrame variables.",
            "distractorRationale": "Option A: Believes more jobs means more parallelism; Spark parallelizes within each shuffle across all executors, so splitting just adds overhead.\n\nOption B: Overestimates Catalyst's cross-subtree merging; it cannot detect that two separately-built DataFrames share the same groupBy.\n\nOption D: Misapplies broadcast intuition; the duplicated shuffle is the cost, not the join — broadcasting the small joined result does not undo the two original shuffles.",
            "conceptsTested": [
              "shuffle counting",
              "stacked aggregates inside one .agg()",
              "Catalyst's optimization scope"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA2-task-04",
      "title": "Wrong Granularity, Plausible Output",
      "type": "find_the_bug",
      "estimatedMinutes": 11,
      "description": {
        "context": "The granularity of a `groupBy` result is determined ENTIRELY by the columns passed as grouping keys — not by the alias on the aggregate, not by the comment above the code. If the date column carries DAILY values like `2026-03-01`, then `groupBy('region', 'date')` produces one row per (region, daily-date) pair — daily granularity, not monthly. To roll up to month, you must TRANSFORM the date first: `groupBy('region', month('date'))` or `date_trunc('month', col('date'))`. This class of bug is silent — every row has correct values, only the row COUNT is wrong.",
        "task": "Which line contains the bug, and what is the underlying mechanism?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Code spec: 'monthly regional consumption' (4 regions × 3 months = 12 rows expected). Actual output: hundreds of rows, each with a correct total_kw for its (region, date)."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum\n\n# Compute monthly consumption per region\nmonthly_regional = (df\n    .groupBy('region', 'date')\n    .agg(sum('daily_kw').alias('total_kw')))\n\nmonthly_regional.write.mode('overwrite').parquet('gold/monthly_regional')"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA2-04-F1",
            "type": "single_select",
            "label": "Which line contains the bug, and what is the underlying mechanism?",
            "options": [
              "Line 5: `groupBy('region', 'date')` groups by DAILY date because the column carries daily values; the result has daily granularity, not monthly. Fix: transform with `month('date')` or `date_trunc('month', col('date'))` before grouping.",
              "Line 6: `.alias('total_kw')` is wrong — the output column should be named `monthly_total_kw` to signal the intended monthly granularity to downstream consumers; the alias is the source of the granularity confusion.",
              "Line 8: `write.mode('overwrite')` is silently appending instead of replacing, so rows from previous daily runs accumulate; the fix is to delete the path before each write to ensure clean monthly output.",
              "There is no bug — the dashboard team should aggregate further at the BI layer; producing daily granularity from this pipeline is correct because it gives consumers maximum flexibility."
            ],
            "correctAnswer": "Line 5: `groupBy('region', 'date')` groups by DAILY date because the column carries daily values; the result has daily granularity, not monthly. Fix: transform with `month('date')` or `date_trunc('month', col('date'))` before grouping.",
            "rationale": "groupBy granularity is determined by the values in the grouping columns. `date` carries daily values like `2026-03-01`, so `groupBy('region', 'date')` produces one row per (region, daily-date) pair — roughly 4 regions × ~90 days = hundreds of rows. The fix is to transform the date column first: `groupBy('region', month('date'))` or `date_trunc('month', col('date'))`. This bug escapes review because every individual row is correct — only the row count is wrong. The most common Junior confusion is treating the alias `total_kw` or the comment `# Compute monthly` as authoritative about granularity; only the groupBy keys decide.",
            "distractorRationale": "Option B: Treats column names as the source of granularity; aliasing the output column does not change row count, only the groupBy keys do.\n\nOption C: Misframes overwrite as append; `mode('overwrite')` replaces the path atomically, no accumulation across runs happens.\n\nOption D: Pushes the fix downstream; the pipeline produces wrong-granularity output regardless of what consumers do with it, which is the bug.",
            "conceptsTested": [
              "groupBy granularity",
              "date truncation",
              "silent semantic bugs"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA3-task-01",
      "title": "Filter Before vs Filter After",
      "type": "spot_the_difference",
      "estimatedMinutes": 11,
      "description": {
        "context": "Filters in PySpark have TWO valid placement points: BEFORE `.groupBy()` (acts on individual rows — like SQL's WHERE) or AFTER `.groupBy().agg()` (acts on aggregate results — like SQL's HAVING). They are NOT interchangeable when the predicate references a row-level column that is consumed by the aggregation. A pre-filter `daily_kw > 0` excludes individual negative readings before they participate. A post-filter on `total_kw > 0` keeps regions whose SUM is positive — even if individual negative readings dragged the sum down. Plus, pre-filtering is faster: fewer rows enter the shuffle.",
        "task": "Which snippet correctly answers the stated goal, and which is more efficient?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Goal: total daily_kw per region using only POSITIVE readings. Production: 500K rows, ~12% zero or negative (sensor errors, prosumer returns)."
        },
        {
          "type": "code_block",
          "label": "Snippet A — filter rows first",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nresult_a = (df\n    .filter(col('daily_kw') > 0)\n    .groupBy('region')\n    .agg(sum('daily_kw').alias('total_kw')))"
        },
        {
          "type": "code_block",
          "label": "Snippet B — filter the aggregated result",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nresult_b = (df\n    .groupBy('region')\n    .agg(sum('daily_kw').alias('total_kw'))\n    .filter(col('total_kw') > 0))"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA3-01-F1",
            "type": "single_select",
            "label": "Which snippet correctly answers the stated goal, and which is more efficient?",
            "options": [
              "Both correct and equal cost — Catalyst pushes the post-aggregation filter on `total_kw` down across the groupBy and rewrites Snippet B into Snippet A's plan, producing identical physical plans.",
              "Snippet A is correct and faster — it filters individual rows before the groupBy, so only positive readings participate in the shuffle and the aggregation; Snippet B sums every reading per region and then drops only regions whose total is non-positive.",
              "Snippet B is correct — the goal mentions 'totals exceeding zero', so the threshold belongs on the aggregate; Snippet A throws away rows that future analysts might need for downstream comparisons against negatives.",
              "Both produce identical output but Snippet B is faster because the post-filter runs on only ~4 region rows, while Snippet A's pre-filter must evaluate a predicate on every one of the 500K input rows."
            ],
            "correctAnswer": "Snippet A is correct and faster — it filters individual rows before the groupBy, so only positive readings participate in the shuffle and the aggregation; Snippet B sums every reading per region and then drops only regions whose total is non-positive.",
            "rationale": "Snippet A answers the stated goal: it excludes individual negative readings BEFORE they participate in the regional sum. Snippet B sums all readings (positive, negative, zero, null) per region first, then drops only regions whose final total is non-positive — a region with mixed positive and negative readings whose sum is still positive remains, with the negatives baked in. Snippet A is also faster: only ~88% of rows enter the shuffle. The most common Junior confusion is treating filter placement as commutative; pre-aggregation and post-aggregation filters answer DIFFERENT questions, with WHERE-vs-HAVING semantics.",
            "distractorRationale": "Option A: Trusts Catalyst to push filters across the aggregation boundary; Catalyst can push pre-agg filters earlier but CANNOT push a filter on a post-aggregation column because `total_kw` does not exist below the aggregation.\n\nOption C: Reverses the semantics; A excludes negative readings, B filters whole regions whose totals are non-positive — different questions.\n\nOption D: Treats per-row predicate evaluation as the dominant cost; the shuffle is the dominant cost, and Snippet A reduces shuffle volume.",
            "conceptsTested": [
              "row-level vs group-level filters",
              "WHERE vs HAVING semantics",
              "early predicate placement"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA3-task-02",
      "title": "HAVING the Right Way",
      "type": "spot_the_difference",
      "estimatedMinutes": 11,
      "description": {
        "context": "When a threshold is a property of the AGGREGATE — for example 'regions whose TOTAL exceeds X' — the filter MUST go AFTER the aggregation. That is SQL's HAVING clause; in PySpark the equivalent is `.groupBy(...).agg(...).filter(...)`. Putting the threshold on individual ROW values asks a different question entirely: 'rows whose individual reading exceeds X', which on real meter data may produce zero rows because no single daily reading is anywhere near a regional-monthly threshold. Always match the predicate's scope to the threshold's scope.",
        "task": "Which snippet answers the stated goal, and what does the other one compute?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Goal: identify regions whose TOTAL monthly daily_kw exceeds 1,000,000 kW. Real residential daily_kw is typically under 50, industrial up to a few thousand."
        },
        {
          "type": "code_block",
          "label": "Snippet A — pre-filter readings above 1,000,000 kW",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nhigh_volume_a = (df\n    .filter(col('daily_kw') > 1_000_000)\n    .groupBy('region')\n    .agg(sum('daily_kw').alias('total_kw')))"
        },
        {
          "type": "code_block",
          "label": "Snippet B — aggregate, then filter the totals",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\nhigh_volume_b = (df\n    .groupBy('region')\n    .agg(sum('daily_kw').alias('total_kw'))\n    .filter(col('total_kw') > 1_000_000))"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA3-02-F1",
            "type": "single_select",
            "label": "Which snippet answers the stated goal, and what does the other one compute?",
            "options": [
              "Snippet A is correct — the 'filter before groupBy' rule says row-level predicates always come first; placing 1,000,000 before the groupBy lets Catalyst push it down and produce regional totals that pass the threshold.",
              "Snippet B is correct — the threshold is a property of the regional TOTAL, which only exists post-aggregation; Snippet A asks a different question and on real meter data (single readings under a few thousand) returns empty.",
              "Both produce the same answer — Catalyst recognises that the filter and aggregate commute when the threshold matches a sum-style aggregate and rewrites Snippet A into Snippet B's plan automatically.",
              "Snippet B is incorrect because filters always come before groupBy for performance; the right pattern is to add a daily threshold like `col('daily_kw') > 100` and let the regional sum exceed 1,000,000 naturally."
            ],
            "correctAnswer": "Snippet B is correct — the threshold is a property of the regional TOTAL, which only exists post-aggregation; Snippet A asks a different question and on real meter data (single readings under a few thousand) returns empty.",
            "rationale": "Snippet B is the PySpark equivalent of SQL's HAVING: aggregate first, then filter the aggregate. The 1,000,000 kW threshold describes the regional total, which exists only AFTER aggregation. Snippet A applies the threshold to individual daily readings; on real NordGrid data (residential under 50 kW, industrial under a few thousand) no single reading exceeds 1,000,000, so Snippet A returns an empty DataFrame. The most common Junior confusion is mechanically applying 'filter before groupBy for performance'; that rule applies only to ROW-level predicates, not to thresholds whose scope IS the aggregate.",
            "distractorRationale": "Option A: Applies the 'filter before groupBy' rule mechanically without checking the predicate's scope; the rule applies to row-level filters, not aggregate-scoped thresholds.\n\nOption C: Trusts Catalyst to commute filter and aggregate; they only commute when the column exists on both sides of the aggregation, which is rarely the case for aggregate thresholds.\n\nOption D: Invents a 'derived daily threshold' that does not match the goal; the goal is regions whose total exceeds 1,000,000, not rows above some other value.",
            "conceptsTested": [
              "HAVING-style filtering",
              "post-aggregation predicates",
              "scale-of-threshold reasoning"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA3-task-03",
      "title": "The Filter That Was Never Pushed",
      "type": "find_the_bug",
      "estimatedMinutes": 11,
      "description": {
        "context": "A filter that references a GROUPING COLUMN (a column passed to `.groupBy()`) is conceptually a row-level filter — the column exists both before and after aggregation, and the predicate's TRUTH value is the same on either side. Such a filter SHOULD be applied BEFORE the groupBy so that non-matching rows never enter the shuffle. Catalyst sometimes pushes these filters automatically, but not reliably across all aggregate forms; writing the filter explicitly before the groupBy is the safe and obvious pattern. Filters on AGGREGATE columns (like `total_kw`) cannot be pushed — they only exist after aggregation.",
        "task": "Which line contains the inefficiency, and what is the cost?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Output values verified correct. Pipeline runs in 4 minutes; comparable single-region pipelines run in ~1 minute. Production has 500K rows across 4 regions and 4 customer_types (~16 groups)."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\n# Generate the NORTH region report\nnorth_report = (df\n    .groupBy('region', 'customer_type')\n    .agg(sum('daily_kw').alias('total_kw'))\n    .filter(col('region') == 'NORTH'))\n\nnorth_report.write.mode('overwrite').parquet('gold/north_report')"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA3-03-F1",
            "type": "single_select",
            "label": "Which line contains the inefficiency, and what is the cost?",
            "options": [
              "Line 7 is fine as written — the filter on `region` is a post-aggregation predicate and Catalyst always pushes such predicates before the groupBy automatically; the runtime cost is at most a few extra milliseconds for the rule rewrite.",
              "Line 7 — the filter references a GROUPING column, not an aggregate, and could safely run before the groupBy. As written, all 500K rows shuffle by (region, customer_type), 16 groups aggregate, then 12 are discarded — wasting roughly 4× the compute.",
              "Line 5 — `groupBy('region', 'customer_type')` should be `groupBy('region')`; the customer_type breakdown is unnecessary detail that quadruples shuffle size and is then discarded by the filter.",
              "Line 9 — `write.mode('overwrite')` should be `write.mode('append')` to avoid the cluster waiting for the directory cleanup phase, which dominates runtime on small outputs and explains the 4× slowdown."
            ],
            "correctAnswer": "Line 7 — the filter references a GROUPING column, not an aggregate, and could safely run before the groupBy. As written, all 500K rows shuffle by (region, customer_type), 16 groups aggregate, then 12 are discarded — wasting roughly 4× the compute.",
            "rationale": "`col('region')` is a grouping column that exists both before and after the aggregation. A predicate on a grouping column is conceptually a row-level filter and can safely run before the groupBy — eliminating ~75% of rows from the shuffle. As written, Catalyst's auto-pushdown is not guaranteed to handle this form, so the pipeline shuffles every row, aggregates ~16 groups, then discards 12 — wasting roughly 4× compute. The most common Junior confusion is treating any post-aggregation filter as 'must be HAVING'; in fact, filters on GROUPING columns are row-level and belong before groupBy.",
            "distractorRationale": "Option A: Trusts Catalyst's automatic pushdown unconditionally; pushdown across aggregations is not always reliable, and the explicit pattern is preferred.\n\nOption C: Targets a non-issue — per-(region, customer_type) totals may be exactly what the consumer wants; the bug is filter placement, not granularity.\n\nOption D: Confuses write modes; `overwrite` is correct for a full report regenerated each run, and directory cleanup is not the source of a 4× slowdown.",
            "conceptsTested": [
              "predicate pushdown",
              "grouping-column filters",
              "Catalyst's reliability boundaries"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA3-task-04",
      "title": "Filtering on the Wrong Column",
      "type": "find_the_bug",
      "estimatedMinutes": 12,
      "description": {
        "context": "After `.agg(sum('daily_kw').alias('total_kw'))`, the post-aggregation schema contains the GROUPING columns plus the ALIASED aggregate column — the original `daily_kw` is consumed and gone. Any downstream `col('daily_kw')` reference fails with `AnalysisException` at plan resolution. Compounding the bug: a broad `try/except Exception` wrapped around the whole pipeline catches the AnalysisException SILENTLY and returns a fallback (unfiltered) result. The pipeline 'works', the operations team sees plausible output, and nobody notices that the filter never ran.",
        "task": "Which line contains the primary bug, and what is the underlying mechanism?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Pipeline runs without errors. Operations reports low-volume groups in output. Console shows: 'Filter failed: cannot resolve `daily_kw`. Did you mean one of: [region, customer_type, total_kw]?' — caught silently."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\ndef compute_high_volume_groups(df):\n    try:\n        result = (df\n            .groupBy('region', 'customer_type')\n            .agg(sum('daily_kw').alias('total_kw'))\n            .filter(col('daily_kw') > 5000))\n        return result\n    except Exception as e:\n        print(f'Filter failed: {e}')\n        return df.groupBy('region', 'customer_type').agg(sum('daily_kw').alias('total_kw'))\n\nhigh_volume = compute_high_volume_groups(df)\nhigh_volume.show()"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA3-04-F1",
            "type": "single_select",
            "label": "Which line contains the primary bug, and what is the underlying mechanism?",
            "options": [
              "Line 8: `.filter(col('daily_kw') > 5000)` references a column the aggregation consumed; the post-aggregation schema is `region, customer_type, total_kw`. Spark raises AnalysisException, which the broad except catches silently and returns the unfiltered fallback.",
              "Line 11: the bare `except Exception` clause is the root bug because catching all exceptions broadly is bad practice; tightening it to `AnalysisException` would surface the real issue but does not fix the broken filter.",
              "Line 7: `.alias('total_kw')` shadows the original `daily_kw` column and breaks downstream filters; removing the alias would let `.filter(col('daily_kw'))` resolve against the un-renamed aggregate.",
              "Line 6: `groupBy('region', 'customer_type')` is missing the `'date'` grouping key; adding a date dimension to the groupBy would cause the filter to resolve correctly because dated rows preserve their pre-aggregation columns."
            ],
            "correctAnswer": "Line 8: `.filter(col('daily_kw') > 5000)` references a column the aggregation consumed; the post-aggregation schema is `region, customer_type, total_kw`. Spark raises AnalysisException, which the broad except catches silently and returns the unfiltered fallback.",
            "rationale": "After `.agg(sum('daily_kw').alias('total_kw'))`, the schema is `region, customer_type, total_kw` — `daily_kw` is gone. The filter on line 8 references `col('daily_kw')` and Spark raises `AnalysisException` at plan resolution. The broad `except Exception` on line 10 catches it, prints a message, and returns the unfiltered groupBy as a fallback — which surfaces in production as 'low-volume groups appearing in a high-volume report'. The most common Junior confusion is treating `try/except Exception` as defensive; here it MASKS the real bug and lets the pipeline ship wrong output silently.",
            "distractorRationale": "Option B: Identifies a real code-quality issue but misses the filter bug; tightening the except would just make the pipeline fail loudly without fixing the broken filter.\n\nOption C: Removing the alias would not fix anything; the column would be named `sum(daily_kw)`, still not `daily_kw`.\n\nOption D: Invents an unrelated grouping requirement; adding `date` does not change the fact that `daily_kw` is consumed by the aggregation.",
            "conceptsTested": [
              "post-aggregation schema",
              "anti-patterns in defensive code",
              "silent failure modes"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA4-task-01",
      "title": "count vs count('column')",
      "type": "output_prediction",
      "estimatedMinutes": 9,
      "description": {
        "context": "PySpark's `count` function has TWO distinct behaviours depending on its argument. `count('*')` counts ROWS — every row, regardless of which columns are null. `count('column_name')` counts non-null VALUES in that column — null entries do not contribute. The difference between `count('*')` and `count('col')` equals exactly the null count of that column, which makes `count('*') - count('col')` a one-line null check. This per-aggregate null policy is uniform across the SQL family but trips up engineers who think 'count is just count'.",
        "task": "What values does `.show()` print for the three count columns?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows total. Engineered nulls: 5 in daily_kw, 2 in region, 3 in customer_type."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import count\n\nresult = df.agg(\n    count('*').alias('rows'),\n    count('daily_kw').alias('daily_kw_count'),\n    count('region').alias('region_count')\n)\nresult.show()"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA4-01-F1",
            "type": "single_select",
            "label": "What values does `.show()` print for the three count columns?",
            "options": [
              "rows=20, daily_kw_count=20, region_count=20 — `count` is uniform across argument forms; both `count('*')` and `count('column')` return the row count of the DataFrame regardless of nulls.",
              "rows=20, daily_kw_count=15, region_count=18 — `count('*')` counts rows (20), `count('col')` counts non-null values; with 5 null daily_kw and 2 null region, the per-column counts are 15 and 18.",
              "rows=15, daily_kw_count=15, region_count=15 — `count('*')` excludes any row that has a null in ANY column; the result is the count of fully-non-null rows, which is the same across all three measurements.",
              "rows=20, daily_kw_count=null, region_count=null — when a column contains any nulls, `count('col')` returns null to flag that the column is incomplete; only `count('*')` returns an integer in that case."
            ],
            "correctAnswer": "rows=20, daily_kw_count=15, region_count=18 — `count('*')` counts rows (20), `count('col')` counts non-null values; with 5 null daily_kw and 2 null region, the per-column counts are 15 and 18.",
            "rationale": "`count('*')` counts every row regardless of nulls (20). `count('column')` counts non-null values in that column: 20 - 5 null daily_kw = 15, and 20 - 2 null region = 18. The difference between the two forms IS the null count for that column, which is why `count('*') - count('col')` is the canonical one-line null check. The most common Junior confusion is treating `count` as one operation; PySpark has two distinct behaviours selected by the argument form.",
            "distractorRationale": "Option A: Holds the 'count is just count' misconception; the argument form selects between row-count and non-null-count behaviours.\n\nOption C: Believes `count('*')` excludes any row with a null in any column; `count('*')` counts rows unconditionally.\n\nOption D: Thinks `count` returns null when the column contains nulls; `count` always returns an integer (zero or positive).",
            "conceptsTested": [
              "count('*') vs count('column')",
              "null counting via subtraction",
              "per-aggregate null policies"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA4-task-02",
      "title": "groupBy on a Nullable Column",
      "type": "output_prediction",
      "estimatedMinutes": 8,
      "description": {
        "context": "PySpark's `groupBy` treats `null` as a SINGLE grouping value — all rows whose grouping column is null collapse into ONE null-keyed group, exactly like rows sharing any other value. This contrasts with `countDistinct` (see JA5.2) and SQL aggregate FUNCTIONS, which IGNORE nulls. The Junior takeaway: every aggregation in PySpark has its own null policy, and `groupBy`'s policy is 'null is a value like any other'. To predict a groupBy row count on a nullable column, count the distinct non-null values AND add 1 if any null rows exist.",
        "task": "What number does `print()` output?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows. customer_type: RESIDENTIAL 8, COMMERCIAL 5, INDUSTRIAL 4, null 3."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "result = df.groupBy('customer_type').count()\nprint(result.count())"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA4-02-F1",
            "type": "single_select",
            "label": "What number does `print()` output?",
            "options": [
              "3 — PySpark's groupBy follows the SQL aggregate-function convention and EXCLUDES rows where the grouping column is null, so only the 3 non-null customer_types form groups.",
              "4 — groupBy treats null as one grouping value like any other, so the result has 3 non-null groups (RESIDENTIAL, COMMERCIAL, INDUSTRIAL) plus one null group containing the 3 null-customer_type rows.",
              "6 — groupBy treats each null as DISTINCT from every other null (NULL = NULL is never true), so each of the 3 null rows becomes its own group, giving 3 non-null groups plus 3 singleton null groups.",
              "20 — groupBy is a row-preserving transformation that attaches a count column to each row without collapsing duplicates; the row count of the source is preserved."
            ],
            "correctAnswer": "4 — groupBy treats null as one grouping value like any other, so the result has 3 non-null groups (RESIDENTIAL, COMMERCIAL, INDUSTRIAL) plus one null group containing the 3 null-customer_type rows.",
            "rationale": "PySpark's `groupBy` treats null as a single grouping value — null rows collapse into one null-keyed group. Result: 4 rows total (RESIDENTIAL, COMMERCIAL, INDUSTRIAL, null with count 3). This contrasts with `countDistinct` and SQL aggregate FUNCTIONS, which IGNORE nulls. The most common Junior confusion is importing the SQL convention 'aggregates skip nulls' to groupBy keys; in PySpark every aggregation has its own null policy, and groupBy's policy is 'null is a value like any other'.",
            "distractorRationale": "Option A: Imports a SQL convention that does not apply to PySpark groupBy keys; null forms its own group, not a dropped row.\n\nOption C: Treats each null as distinct from every other null; PySpark equality is null-safe in the grouping context, so all nulls collapse into one group.\n\nOption D: Confuses groupBy with a row-preserving transformation; groupBy collapses rows that share the grouping key.",
            "conceptsTested": [
              "null as a grouping key",
              "PySpark vs SQL null semantics",
              "per-function null policies"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA4-task-03",
      "title": "The Wrong Average",
      "type": "find_the_bug",
      "estimatedMinutes": 10,
      "description": {
        "context": "When you compute an average manually as `sum(col) / count(...)`, the NUMERATOR and DENOMINATOR must agree on null policy. PySpark's `sum('daily_kw')` IGNORES nulls (standard SQL semantics — null contributes nothing). But `count('*')` counts EVERY row, including rows where `daily_kw` is null. Mixing these gives a denominator larger than the numerator's effective row count, producing an average that is too LOW. The correct manual form is `sum('daily_kw') / count('daily_kw')` — both null-aware. Or just use built-in `avg('daily_kw')`, which already does this correctly.",
        "task": "Which line contains the bug, and what is the mechanism?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows; 5 with null daily_kw. Operations independently used `avg()` and got values ~25% higher than this pipeline produces."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, count, col\n\n# Compute average daily consumption per region\naverages = (df\n    .groupBy('region')\n    .agg(\n        sum('daily_kw').alias('total'),\n        count('*').alias('n')\n    )\n    .withColumn('avg_kw', col('total') / col('n')))\n\naverages.select('region', 'avg_kw').show()"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA4-03-F1",
            "type": "single_select",
            "label": "Which line contains the bug, and what is the mechanism?",
            "options": [
              "Line 8: `count('*')` counts ALL rows including those with null daily_kw, while `sum('daily_kw')` on line 7 ignores nulls — the denominator is larger than the numerator's row count, producing an average ~25% too low.",
              "Line 11: the division should use `coalesce(col('n'), 1)` to prevent null denominators that produce null averages; without coalesce a region with no rows would yield a null avg_kw.",
              "Line 7: `sum('daily_kw')` silently treats null values as zero, dragging the total down by the null count's worth of zeros and depressing the average from below.",
              "Line 5: `groupBy('region')` misses null-region rows because Spark drops them by default; the missing rows would have raised the regional totals if included."
            ],
            "correctAnswer": "Line 8: `count('*')` counts ALL rows including those with null daily_kw, while `sum('daily_kw')` on line 7 ignores nulls — the denominator is larger than the numerator's row count, producing an average ~25% too low.",
            "rationale": "The bug is denominator/numerator misalignment on null policy. `sum('daily_kw')` IGNORES nulls (PySpark aggregate functions skip nulls, like SQL SUM). `count('*')` counts EVERY row regardless of nulls. With 5 of 20 daily_kw values null, the manual avg = (sum of 15 values) / 20 — about 75% of the correct mean. Fix: `count('daily_kw')` to count only non-null rows, OR just use `avg('daily_kw')` which is already correct. The most common Junior confusion is treating `count('*')` and `count('col')` as interchangeable; their null policies differ.",
            "distractorRationale": "Option B: `count('*')` always returns an integer ≥ 0; the bug is the denominator being TOO LARGE, not null.\n\nOption C: Reverses `sum`'s actual null policy; PySpark `sum()` ignores nulls entirely (does not treat them as zero), identical to SQL SUM.\n\nOption D: Blames null-region handling; PySpark groupBy includes null groups, and this bug is about null daily_kw values, not null region values.",
            "conceptsTested": [
              "count vs count(column) in averaging",
              "manual vs built-in avg()",
              "denominator alignment with numerator's null policy"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA4-task-04",
      "title": "The Disappearing Group",
      "type": "find_the_bug",
      "estimatedMinutes": 12,
      "description": {
        "context": "A `.filter(col('x').isNotNull())` looks like a harmless defensive check, but it is DESTRUCTIVE: every row whose `x` is null is removed and never seen again by the pipeline. If those rows represent real measurements with metadata gaps (a meter whose region tag was lost upstream still produced real kW readings), the pipeline silently UNDER-REPORTS by the size of the null population. The right pattern is to ROUTE null-keyed rows to a quarantine path (or use `coalesce(col, lit('UNKNOWN'))` as an explicit bucket) so they remain countable and visible — never to drop them silently.",
        "task": "Which line contains the bug, and what is the production consequence?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Pipeline runs cleanly. Output has 4 rows (NORTH/SOUTH/EAST/WEST). Production has ~240 meters with null region. Regulatory totals run ~2-3% lower than independent reconciliation, consistently."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import sum, col\n\n# Defensive filter: drop any row with null region\n# (added after the dashboard crashed on a null value)\nregional_totals = (df\n    .filter(col('region').isNotNull())\n    .groupBy('region')\n    .agg(sum('daily_kw').alias('total_kw')))\n\n# Write to the regulatory reporting layer\nregional_totals.write.mode('overwrite').parquet('gold/regional_consumption')"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA4-04-F1",
            "type": "single_select",
            "label": "Which line contains the bug, and what is the production consequence?",
            "options": [
              "Line 11: writing directly to gold/ skips a silver layer where data quality checks would catch the under-reporting; the architectural shortcut is the root cause of the regulatory drift.",
              "Line 6: `filter(col('region').isNotNull())` silently drops ~240 meters with null region tags — those rows carry real daily_kw, so the regulatory total is short by exactly that consumption (~2-3%).",
              "Line 8: `sum('daily_kw')` is missing a `coalesce(col('daily_kw'), 0)` wrapper to handle nulls in the value column; the missing coalesce drops null-daily_kw rows from the totals.",
              "Line 7: `groupBy('region')` excludes null-region rows by default in Spark 3.5+; this is a recent behaviour change and the pipeline needs to set `spark.sql.legacy.groupByNullSafe=true` to include them again."
            ],
            "correctAnswer": "Line 6: `filter(col('region').isNotNull())` silently drops ~240 meters with null region tags — those rows carry real daily_kw, so the regulatory total is short by exactly that consumption (~2-3%).",
            "rationale": "The defensive filter on line 6 looks reasonable but is destructive: every meter with a null region tag is removed, taking its real daily_kw with it. NordGrid has ~240 such meters due to upstream tagging gaps — their consumption is real, but the pipeline never sees it. Regulatory totals are short by exactly that consumption (~2-3%). The fix is to route null-region rows to a quarantine path or bucket them under `UNKNOWN` via `coalesce(col('region'), lit('UNKNOWN'))`. The most common Junior confusion is treating `isNotNull()` filters as harmless hygiene; they are silent data drops with real production consequences.",
            "distractorRationale": "Option A: Identifies an architecture concern but misses the data-loss bug; even a properly-layered pipeline that drops null regions still under-reports by the same amount.\n\nOption C: Reverses `sum`'s null policy; `sum()` ignores nulls (does not need coalesce), and null-daily_kw is a separate population from null-region.\n\nOption D: Invents a Spark version behaviour change; PySpark groupBy has consistently included null groups, and there is no `legacy.groupByNullSafe` setting that toggles this.",
            "conceptsTested": [
              "silent data drop",
              "defensive filtering anti-pattern",
              "quarantine paths and visibility"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA5-task-01",
      "title": "Three Ways to Count Distinct",
      "type": "spot_the_difference",
      "estimatedMinutes": 12,
      "description": {
        "context": "PySpark offers THREE ways to count distinct values, each with a distinct cost profile. `distinct().count()` shuffles ALL rows by the column to deduplicate, then counts — TWO actions, two passes through the plan. `countDistinct()` shuffles by the column once and produces the exact count in a single aggregate. `approx_count_distinct()` uses HyperLogLog: each partition computes a small probabilistic sketch and the sketches merge in a single reduce step — NO shuffle of the data itself. The ordering by cost is `approx_count_distinct < countDistinct < distinct().count()`, with a roughly 100× gap between the cheapest and the most expensive at large scale.",
        "task": "Which ranking from CHEAPEST to MOST EXPENSIVE is correct, and what mechanism drives the difference?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Goal: count distinct meter_ids on the full 500K-row dataset. All three snippets return values within ~0.5% of the true distinct count."
        },
        {
          "type": "code_block",
          "label": "Snippet A — distinct, then count",
          "language": "python",
          "content": "result_a = df.select('meter_id').distinct().count()"
        },
        {
          "type": "code_block",
          "label": "Snippet B — countDistinct inside agg",
          "language": "python",
          "content": "from pyspark.sql.functions import countDistinct\n\nresult_b = df.agg(countDistinct('meter_id')).first()[0]"
        },
        {
          "type": "code_block",
          "label": "Snippet C — approximate distinct",
          "language": "python",
          "content": "from pyspark.sql.functions import approx_count_distinct\n\nresult_c = df.agg(approx_count_distinct('meter_id')).first()[0]"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA5-01-F1",
            "type": "single_select",
            "label": "Which ranking from cheapest to most expensive is correct, and what mechanism drives the difference?",
            "options": [
              "A < B < C — `distinct().count()` is cheapest because deduplication runs as a narrow scan, then count is local; `approx_count_distinct` is most expensive because it runs HyperLogLog overhead on top of an exact count for verification.",
              "C < B < A — `approx_count_distinct` uses HyperLogLog sketches that merge in a single reduce with no full shuffle; `countDistinct` shuffles once; `distinct().count()` shuffles AND runs two actions, re-executing the upstream plan.",
              "B < A < C — `countDistinct` is cheapest because it inlines into a single aggregate; `distinct().count()` is middle because dedup is fast; `approx_count_distinct` is slowest because the sketches must be built per executor before merging.",
              "All three have identical cost — Catalyst recognises that the three forms answer the same question and rewrites them into a single physical plan, so the engineer's choice is purely stylistic at runtime."
            ],
            "correctAnswer": "C < B < A — `approx_count_distinct` uses HyperLogLog sketches that merge in a single reduce with no full shuffle; `countDistinct` shuffles once; `distinct().count()` shuffles AND runs two actions, re-executing the upstream plan.",
            "rationale": "The ranking is C < B < A. `approx_count_distinct` uses HyperLogLog: each partition computes a small fixed-size probabilistic sketch, and sketches merge in a single reduce — no full shuffle of the data. `countDistinct` shuffles all rows by the column once and produces an exact count. `distinct().count()` shuffles to deduplicate AND runs two actions (distinct, then count) — Spark re-executes the upstream plan for the second action because intermediate DataFrames are not auto-cached. The most common Junior confusion is treating 'approximate' as 'extra overhead'; in practice approximation is what lets HyperLogLog SKIP the shuffle entirely.",
            "distractorRationale": "Option A: Reverses the ranking by treating approximation as overhead; HyperLogLog avoids the shuffle entirely, which is the dominant cost.\n\nOption C: Imagines `approx_count_distinct` runs the exact computation as a verification step; it does not — it ONLY computes sketches.\n\nOption D: Trusts Catalyst to unify three semantically distinct operations; Catalyst preserves the engineer's choice because exact and approximate are different functions with different guarantees.",
            "conceptsTested": [
              "HyperLogLog vs exact dedup",
              "shuffle as the dominant cost",
              "actions vs transformations re-execution"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA5-task-02",
      "title": "countDistinct with Nulls",
      "type": "output_prediction",
      "estimatedMinutes": 8,
      "description": {
        "context": "PySpark splits null handling between TWO conventions: aggregate FUNCTIONS (sum, count, countDistinct, avg, min, max) IGNORE nulls — they never contribute, never form a bucket. GROUPING (groupBy) treats null as a value like any other — null rows form a null-keyed group. So `countDistinct('meter_id')` counts distinct NON-NULL values only, while `groupBy('meter_id').count()` would include a null group. To count nulls as a distinct value, you must bucket them explicitly: `countDistinct(coalesce(col('meter_id'), lit('NULL_PLACEHOLDER')))`.",
        "task": "What number does `print()` output?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "20 rows. meter_id: 14 unique non-null values; 3 rows where meter_id is null. Some non-null IDs repeat (M-001 ×3, M-002 ×2)."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import countDistinct\n\nresult = df.agg(countDistinct('meter_id')).first()[0]\nprint(result)"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA5-02-F1",
            "type": "single_select",
            "label": "What number does `print()` output?",
            "options": [
              "14 — `countDistinct` is an aggregate function and IGNORES nulls; it counts only the 14 distinct non-null meter_ids, treating the 3 null rows as if they were not there at all.",
              "15 — `countDistinct` follows the groupBy convention where null is a value like any other; the 14 non-null IDs plus 'null' as a 15th distinct value gives 15.",
              "17 — `countDistinct` treats each null as distinct from every other null (NULL = NULL is never true), so the 3 null rows each become their own distinct value, yielding 14 + 3 = 17.",
              "20 — `countDistinct` returns the source row count when ties exist because Spark falls back to row counting when distinct comparison is ambiguous due to nulls."
            ],
            "correctAnswer": "14 — `countDistinct` is an aggregate function and IGNORES nulls; it counts only the 14 distinct non-null meter_ids, treating the 3 null rows as if they were not there at all.",
            "rationale": "`countDistinct` IGNORES nulls — it counts only distinct non-null values, so the answer is 14 (the 3 null rows do not contribute). This contrasts with groupBy's behaviour in JA4.2 where null forms its own group. The PySpark convention is split: aggregate FUNCTIONS exclude nulls (SQL-style), GROUPING includes nulls. To count nulls as a distinct value, bucket them explicitly: `countDistinct(coalesce(col('meter_id'), lit('NULL_PLACEHOLDER')))`. The most common Junior confusion is importing the groupBy convention (null is a value) into countDistinct; the two functions live on opposite sides of the null-policy split.",
            "distractorRationale": "Option B: Imports the groupBy convention into countDistinct; countDistinct follows the aggregate-function convention and skips nulls.\n\nOption C: Treats each null as distinct from every other null; countDistinct does not see nulls at all, regardless of how many there are.\n\nOption D: Conflates countDistinct with row count; countDistinct never returns the row count except in the trivial case where every row has a unique non-null value.",
            "conceptsTested": [
              "null policies in countDistinct vs groupBy",
              "explicit null bucketing pattern",
              "function-level vs grouping-level null semantics"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA5-task-03",
      "title": "Distinct After vs Distinct Inside",
      "type": "spot_the_difference",
      "estimatedMinutes": 11,
      "description": {
        "context": "The SCOPE of `distinct` matters. `df.distinct().groupBy('region').count()` deduplicates rows GLOBALLY across the entire DataFrame first, then counts surviving rows per region — so a row appearing in both NORTH and SOUTH (e.g., due to a region-tagging bug) is collapsed to ONE row total before counting. By contrast, `groupBy('region').agg(countDistinct(struct(...)))` counts distinct combinations WITHIN each group — the same row spanning two regions counts once per region. These produce different numbers whenever duplicates cross group boundaries.",
        "task": "Which snippet correctly answers 'unique meter readings per region', where 'same reading' means every column matches?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Input may contain duplicate rows from upstream SCADA replays. 'Same reading' = every column matches. Region-tagging bugs occasionally place the same reading in two regions."
        },
        {
          "type": "code_block",
          "label": "Snippet A — deduplicate first, then group and count",
          "language": "python",
          "content": "result_a = (df\n    .distinct()\n    .groupBy('region')\n    .count())"
        },
        {
          "type": "code_block",
          "label": "Snippet B — group first, then count distinct rows per group",
          "language": "python",
          "content": "from pyspark.sql.functions import countDistinct, struct, col\n\nresult_b = (df\n    .groupBy('region')\n    .agg(countDistinct(\n        struct(*[c for c in df.columns if c != 'region'])\n    ).alias('count')))"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA5-03-F1",
            "type": "single_select",
            "label": "Which snippet correctly answers the stated goal?",
            "options": [
              "Both produce identical output — `distinct()` and `countDistinct(struct(...))` are semantically equivalent because they both remove duplicate row signatures, just at different stages of the pipeline.",
              "Snippet A is correct — `distinct()` deduplicates GLOBALLY before grouping, so a row that spans two regions due to a tagging bug is collapsed once total; per-region count then reflects unique-globally readings.",
              "Snippet B is correct — `countDistinct` on a struct of all non-region columns is the only form that handles row-level uniqueness within PySpark's aggregate machinery; `distinct()` is a transformation that drops null-bearing rows.",
              "Snippet A returns wrong results when daily_kw is null because `distinct()` treats null != null for deduplication, so duplicate null-bearing rows survive and inflate the count."
            ],
            "correctAnswer": "Snippet A is correct — `distinct()` deduplicates GLOBALLY before grouping, so a row that spans two regions due to a tagging bug is collapsed once total; per-region count then reflects unique-globally readings.",
            "rationale": "Snippet A deduplicates rows GLOBALLY (every column must match) before grouping — so a row that appears in both NORTH and SOUTH due to a tagging bug becomes ONE row, then groupBy/count assigns it to whichever region survived. Snippet B counts distinct row-combinations WITHIN each region — the same cross-region duplicate counts once per region. They diverge whenever duplicates cross group boundaries. The most common Junior confusion is treating distinct-and-count as syntactic alternatives; their scope of 'distinct' (global vs within-group) gives genuinely different answers.",
            "distractorRationale": "Option A: Treats the two snippets as syntactically equivalent; their scope of 'distinct' is different (global vs within-group), producing different counts whenever duplicates span groups.\n\nOption C: Reverses which form matches the goal; countDistinct-of-struct counts distinct-within-group, not distinct-globally.\n\nOption D: Invents a false null behaviour; PySpark `distinct()` treats null = null for deduplication purposes (null-safe equality in this context).",
            "conceptsTested": [
              "pre-aggregation vs in-aggregation distinct",
              "scope of 'distinct'",
              "cross-group duplicates"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA5-task-04",
      "title": "When Approximate Was the Wrong Choice",
      "type": "find_the_bug",
      "estimatedMinutes": 11,
      "description": {
        "context": "`approx_count_distinct` is a PROBABILISTIC estimator with a default relative standard deviation (rsd) of 0.05 — each per-group result is typically within 5% of the true value, but never exactly correct. That tradeoff is fine for DASHBOARDS where 'roughly how many' is the question. It is NOT fine for RECONCILIATION reports (billing, regulatory, audit) where 'exactly how many' is the question. Per-group errors do not cancel cleanly when aggregated across many groups; small biases accumulate into a non-zero overall bias. The Junior rule: choose `approx_count_distinct` for performance dashboards and `countDistinct` for systems of record.",
        "task": "Which line contains the bug, and what is the underlying mechanism?"
      },
      "evidence": [
        {
          "type": "text",
          "title": "Input sample",
          "content": "Pipeline runs in 90s. Per-group values within ~5% of truth. Monthly total ~2% lower than billing's independent count, consistently across months."
        },
        {
          "type": "code_block",
          "label": "snippet.py",
          "language": "python",
          "content": "from pyspark.sql.functions import approx_count_distinct\n\n# Daily meter-count reconciliation per substation\n# (used by billing for monthly reconciliation)\nreconciliation = (df\n    .groupBy('substation_id', 'date')\n    .agg(approx_count_distinct('meter_id').alias('meter_count')))\n\nreconciliation.write.mode('overwrite').parquet('gold/billing_reconciliation')"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA5-04-F1",
            "type": "single_select",
            "label": "Which line contains the bug, and what is the underlying mechanism?",
            "options": [
              "Line 7: `approx_count_distinct` has ~5% relative error per group; for billing reconciliation where exact counts matter, the per-group bias accumulates across many groups into the ~2% monthly drift. Fix: `countDistinct`.",
              "Line 6: `groupBy('substation_id', 'date')` is the wrong granularity — billing reconciliation should aggregate at the monthly level, not daily; the daily granularity multiplies error compared to monthly.",
              "Line 7: `.alias('meter_count')` is named incorrectly — downstream billing code expects `distinct_meter_count` and silently joins on the wrong column, producing the ~2% drift.",
              "Line 9: `write.mode('overwrite')` clobbers historical reconciliations; each day's run replaces the previous day's output and the billing team's monthly aggregator misses the dropped days."
            ],
            "correctAnswer": "Line 7: `approx_count_distinct` has ~5% relative error per group; for billing reconciliation where exact counts matter, the per-group bias accumulates across many groups into the ~2% monthly drift. Fix: `countDistinct`.",
            "rationale": "`approx_count_distinct` is a probabilistic estimator with default rsd 0.05 — each per-group value is within ~5% of true. For a dashboard, that is fine. For billing reconciliation that aggregates across substations and days into a monthly total, small per-group biases do not cancel cleanly; they accumulate into a ~2% systemic drift. Fix: `countDistinct('meter_id')`, exact at the cost of a real shuffle. The most common Junior confusion is copying patterns across teams ('the dashboard team uses it'); the right function depends on whether downstream consumers can tolerate approximation.",
            "distractorRationale": "Option B: Targets granularity; daily granularity is correct for daily reconciliation, and rolling up to monthly does not solve the per-group error.\n\nOption C: Misframes alias as the cause; `.alias()` does not silently break joins — a name mismatch would raise AnalysisException, not produce ~2% drift.\n\nOption D: Confuses write modes; `overwrite` is correct for a daily snapshot, and the billing aggregator reads each day independently anyway.",
            "conceptsTested": [
              "approximate vs exact tradeoff",
              "use-case-driven function choice",
              "compounding bias across groups"
            ]
          }
        ]
      }
    },
    {
      "id": "module-JA6-task-01",
      "title": "Daily Regional Consumption Pipeline",
      "type": "refactor_reading",
      "estimatedMinutes": 18,
      "description": {
        "context": "Reading aggregation pipelines is mostly about classifying each line: is it a NARROW transformation (read, withColumn, filter — runs locally on each partition, no shuffle) or a WIDE one (groupBy, join — requires redistributing data across executors)? Joins to a small dimension table get broadcast and skip the shuffle; joins to a large dimension shuffle both sides. Defensive `.filter(col.isNotNull())` patterns silently drop data and almost always need a quarantine path. Order of narrow operations on disjoint columns does not matter — Catalyst will reorder them anyway. These four questions exercise each of those reading skills against a real-looking pipeline.",
        "task": "Read the pipeline carefully (line numbers shown), then answer the questions below."
      },
      "evidence": [
        {
          "type": "code_block",
          "label": "daily_regional_pipeline.py",
          "language": "python",
          "content": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, sum, to_date, when, lit\n\nspark = SparkSession.builder.appName(\"daily_regional\").getOrCreate()\n\n# Bronze: read raw meter readings\nmeters = (spark.read\n    .option(\"header\", True)\n    .option(\"inferSchema\", True)\n    .csv(\"bronze/meters/2026-03-*/\"))\n\n# Cast date string to proper DateType\nmeters = meters.withColumn(\"date\", to_date(col(\"date\"), \"yyyy-MM-dd\"))\n\n# Drop obvious garbage (sensor errors flag negative readings)\nmeters = meters.filter(col(\"daily_kw\").isNotNull() & (col(\"daily_kw\") >= 0))\n\n# Substation reference table (Silver layer, already cleansed)\nsubstations = spark.read.parquet(\"silver/substations/\")\n\n# Enrich meter readings with substation metadata\nenriched = meters.join(\n    substations.select(\"substation_id\", \"name\", \"capacity_mw\"),\n    on=\"substation_id\",\n    how=\"left\"\n)\n\n# Aggregate to daily totals per region\ndaily_totals = (enriched\n    .groupBy(\"region\", \"date\")\n    .agg(sum(\"daily_kw\").alias(\"total_kw\"))\n    .withColumn(\"level_flag\",\n        when(col(\"total_kw\") > 1000000, lit(\"HIGH\"))\n        .otherwise(lit(\"NORMAL\"))))\n\n# Write to Silver\ndaily_totals.write.mode(\"overwrite\").partitionBy(\"date\").parquet(\"silver/daily_regional/\")\n"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA6-01-Q1",
            "type": "single_select",
            "label": "Q1 — Which line triggers the first shuffle of meter data across executors?",
            "options": [
              "Line 11 — the CSV read with `csv()` shuffles to balance partitions across executors before any further processing.",
              "Line 14 — the `withColumn` with `to_date` shuffles because it changes the column's dtype and Spark redistributes by the new type.",
              "Line 23 — the `join` is the first wide transformation; both meters and substations must be redistributed by `substation_id` so matching keys land on the same executor.",
              "Line 31 — the `groupBy` is the first shuffle; everything before it is narrow because joins to small dimensions are pure broadcasts and never shuffle."
            ],
            "correctAnswer": "Line 23 — the `join` is the first wide transformation; both meters and substations must be redistributed by `substation_id` so matching keys land on the same executor.",
            "rationale": "The first shuffle is the join on line 23. CSV reads (line 11) are narrow file scans. The `withColumn` (line 14) and `filter` (line 17) are narrow transformations applied locally per partition. The join requires shuffling both meters and substations by `substation_id` — that is the first WIDE transformation. The groupBy on line 31 triggers a second shuffle (by region, date), but the question asks about the FIRST. The most common Junior confusion is treating CSV reads or `withColumn` as wide; only operations that REDISTRIBUTE data by key shuffle.",
            "distractorRationale": "Option A: Treats CSV reads as wide; reads produce partitions based on file layout but do not shuffle.\n\nOption B: Treats `withColumn` as wide; type changes are narrow per-row operations applied locally.\n\nOption D: Imagines all small-dimension joins broadcast automatically; substations may broadcast at runtime, but the QUESTION about 'first wide transformation' is answered by the logical plan, where `join` is wide before the broadcast optimization is decided."
          },
          {
            "id": "JA6-01-Q2",
            "type": "single_select",
            "label": "Q2 — What happens to a meter row whose substation_id has no match in the substations table?",
            "options": [
              "Dropped from the output — even though `how='left'`, Spark removes left rows without a right-side match because the downstream groupBy does not handle null metadata, causing silent data loss.",
              "Survives the join with null metadata — `how='left'` preserves every meter row; orphan substations populate name/capacity_mw with null but daily_kw and region are intact and still contribute to the regional total.",
              "Raises NullPointerException at the join site — Spark fails fast when a join key has no match, forcing the engineer to either filter pre-join or specify a default value.",
              "Routed to a Spark-default null-quarantine path — Spark automatically writes orphan-key rows to a side output named `_unmatched/` so the engineer can investigate."
            ],
            "correctAnswer": "Survives the join with null metadata — `how='left'` preserves every meter row; orphan substations populate name/capacity_mw with null but daily_kw and region are intact and still contribute to the regional total.",
            "rationale": "`how='left'` on line 25 keeps EVERY left-side (meter) row. When a substation_id has no match in the dimension, the join populates the right-side columns (name, capacity_mw) with null but keeps every left-side column intact — including daily_kw and region. The aggregation on line 31 groups by region/date and the orphan reading contributes its daily_kw to the regional total. This is generally desirable for a consumption report — readings are real even when metadata is incomplete. The most common Junior confusion is conflating `left` with `inner`; left preserves left, inner drops unmatched.",
            "distractorRationale": "Option A: Conflates left join with inner join; left preserves all left rows regardless of right-side match.\n\nOption C: Imagines Spark fails on missing matches; Spark uses null to represent missing matches in outer joins.\n\nOption D: Invents a Spark-default quarantine path; no such automatic mechanism exists, the engineer must build quarantine paths explicitly."
          },
          {
            "id": "JA6-01-Q3",
            "type": "single_select",
            "label": "Q3 — Could the `.withColumn` on line 14 (date cast) and the `.filter` on line 17 (drop negatives) swap places without changing the output?",
            "options": [
              "Yes — both are narrow transformations operating on disjoint columns (date vs daily_kw); they commute, output is identical, and Catalyst would likely reorder them anyway based on cost.",
              "No — the filter must come first because filters always reduce rows before downstream operations, so swapping would produce incorrect output by casting unfiltered rows.",
              "No — swapping raises AnalysisException because Catalyst requires type-conversions to happen before any filter, otherwise the filter cannot be type-checked at plan time.",
              "Yes, but only if `date` is already DateType — the cast is a no-op in that case so the filter sees the same data either way; otherwise swapping changes output."
            ],
            "correctAnswer": "Yes — both are narrow transformations operating on disjoint columns (date vs daily_kw); they commute, output is identical, and Catalyst would likely reorder them anyway based on cost.",
            "rationale": "Both operations are narrow and act on disjoint columns: `to_date` rewrites `date`, `filter` reads `daily_kw`. They commute — output is identical regardless of order. Catalyst would likely reorder them based on cost (filtering first to reduce input size for the cast). The Junior principle: narrow transformations on disjoint columns commute, and visible code order does not affect correctness when no transformation depends on another's output. The most common Junior confusion is reading code order as semantic order; in lazy evaluation, the engineer's order is just a starting point for the optimizer.",
            "distractorRationale": "Option B: Treats code order as performance-mandatory; Catalyst reorders narrow transformations independently of source order, so 'must come first' is wrong.\n\nOption C: Invents a type-checking requirement; Spark type-checks the plan as a whole, not in source order.\n\nOption D: Adds a conditional that does not change the answer; the cast is idempotent on DateType, but the commutativity holds either way."
          },
          {
            "id": "JA6-01-Q4",
            "type": "single_select",
            "label": "Q4 — Which line is most likely to draw a comment from a junior reviewer who has read PS6?",
            "options": [
              "Line 4 — the SparkSession `.appName(...)` is non-descriptive; production code should use a project-prefixed name like `nordgrid.silver.daily_regional` for traceability in the Spark UI.",
              "Line 17 — `.filter(col('daily_kw').isNotNull() & col('daily_kw') >= 0)` silently drops both nulls and negatives without any quarantine path or null-count metric, losing the sensor-failure and decommissioning signals.",
              "Line 25 — `how='left'` keeps orphan-substation readings; for a regional consumption report this is acceptable, but the reviewer would call it out as 'inner would be safer'.",
              "Line 35 — `.partitionBy('date')` creates one Parquet file per date; for a daily report this produces excessive small-file fragmentation and should be removed."
            ],
            "correctAnswer": "Line 17 — `.filter(col('daily_kw').isNotNull() & col('daily_kw') >= 0)` silently drops both nulls and negatives without any quarantine path or null-count metric, losing the sensor-failure and decommissioning signals.",
            "rationale": "Line 17 survives review on most teams but should not. Filtering null and negative daily_kw is reasonable for a consumption total — but doing it SILENTLY, with no quarantine path or null-count metric, is the JA4.4 pattern. A meter that consistently reports null daily_kw is either decommissioned or has a sensor problem; dropping the row without surfacing it loses both signals. The fix is to count the dropped rows, log the count, or route them to a quarantine path. The most common Junior confusion is treating defensive `.isNotNull()` filters as harmless hygiene; they are silent data drops with downstream consequences.",
            "distractorRationale": "Option A: Targets a stylistic concern, not a correctness one; appName affects monitoring but not output.\n\nOption C: Identifies a correct line as wrong; left join here is intentional and preserves orphan-substation readings, which is the right behaviour for a consumption report.\n\nOption D: Misframes partitionBy on a date column; partitioning Parquet by date is a standard layout for time-series data and is not the source of small-file fragmentation in this pipeline."
          }
        ]
      }
    },
    {
      "id": "module-JA6-task-02",
      "title": "The Monthly Billing Aggregation",
      "type": "refactor_reading",
      "estimatedMinutes": 20,
      "description": {
        "context": "Counting shuffles in a pipeline is a Junior-level exercise: every `groupBy`, every join (unless broadcast), every `distinct`, every `repartition` is one shuffle. `count('*')` counts all rows in a group (including null values in any column); `count('col')` counts only non-null values in `col` — the semantic difference matters for column aliases like 'days_reported' or 'readings'. `groupBy` produces NO row for groups absent from the input — to bill a meter that produced zero readings in a month, the engineer must scaffold a complete (meter × month) cross-join. `coalesce(col, lit(0))` null-guards arithmetic against null propagation. These five questions exercise those reading skills.",
        "task": "Read the pipeline carefully (line numbers shown), then answer all questions below."
      },
      "evidence": [
        {
          "type": "code_block",
          "label": "monthly_billing.py",
          "language": "python",
          "content": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import (\n    col, sum, count, avg, when, lit, month, year, coalesce\n)\n\nspark = SparkSession.builder.appName(\"monthly_billing\").getOrCreate()\n\n# Read meter readings (Silver, already cleansed)\nmeters = spark.read.parquet(\"silver/meters/\")\n\n# Read tariff reference table\ntariffs = spark.read.parquet(\"silver/tariffs/\")\n\n# Aggregate per (meter_id, month) — the per-meter monthly bill basis\nmonthly_per_meter = (meters\n    .withColumn(\"year\", year(col(\"date\")))\n    .withColumn(\"month\", month(col(\"date\")))\n    .groupBy(\"meter_id\", \"customer_type\", \"year\", \"month\")\n    .agg(\n        sum(\"daily_kw\").alias(\"total_kw\"),\n        count(\"*\").alias(\"days_reported\"),\n        avg(\"daily_kw\").alias(\"avg_daily_kw\")\n    ))\n\n# Look up the customer's tariff and compute the bill\nbilled = monthly_per_meter.join(tariffs, on=\"customer_type\", how=\"left\")\n\n# Defensive: if total_kw is null (meter reported no readings), bill is zero\nbilled = billed.withColumn(\n    \"bill_eur\",\n    coalesce(col(\"total_kw\"), lit(0)) * coalesce(col(\"eur_per_kw\"), lit(0))\n)\n\n# Per-region monthly summary for the finance dashboard\n# (joins back to meter table to get region — meter_id is the key)\nregion_lookup = meters.select(\"meter_id\", \"region\").distinct()\nbilled_with_region = billed.join(region_lookup, on=\"meter_id\", how=\"left\")\n\nmonthly_regional = (billed_with_region\n    .groupBy(\"region\", \"year\", \"month\")\n    .agg(\n        sum(\"bill_eur\").alias(\"total_billed_eur\"),\n        count(\"*\").alias(\"bills_issued\")\n    ))\n\nmonthly_regional.write.mode(\"overwrite\").parquet(\"gold/monthly_regional_billing/\")\n"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA6-02-Q1",
            "type": "single_select",
            "label": "Q1 — How many shuffles does this pipeline trigger?",
            "options": [
              "1 — only the final groupBy on line 41 shuffles; everything else (joins to small dimensions, distinct on a small column, the per-meter aggregate) avoids shuffles via broadcasting and pushdown.",
              "2 — the groupBy on line 18 and the groupBy on line 41 are the two wide transformations; joins always broadcast and distinct is local within each partition.",
              "4 — the two groupBy operations plus the two joins; distinct deduplicates within each partition without crossing the network.",
              "5 — groupBy line 18, join line 26, distinct line 36, join line 37, groupBy line 41; each redistributes data by some key and counts as one shuffle."
            ],
            "correctAnswer": "5 — groupBy line 18, join line 26, distinct line 36, join line 37, groupBy line 41; each redistributes data by some key and counts as one shuffle.",
            "rationale": "Five shuffles total. Wide transformations in this pipeline: groupBy line 18 (by meter_id, customer_type, year, month), join line 26 (by customer_type — may broadcast if tariffs is small), distinct line 36 (deduplicates by meter_id, region), join line 37 (by meter_id), groupBy line 41 (by region, year, month). Five wide ops = five shuffles. The shuffle count itself is a code-review concern at this scale. The most common Junior confusion is forgetting that `distinct` shuffles (it is implemented as groupBy with no aggregate) and assuming all small-dimension joins broadcast.",
            "distractorRationale": "Option A: Treats joins as always broadcast and distinct as always local; both shuffle in the general case.\n\nOption B: Counts only groupBy as shuffles; joins and distinct also shuffle.\n\nOption C: Forgets that `distinct` is a wide transformation implemented as groupBy with no aggregate — it shuffles by the columns being deduplicated."
          },
          {
            "id": "JA6-02-Q2",
            "type": "single_select",
            "label": "Q2 — What does `count('*')` on line 21 actually count?",
            "options": [
              "Total bills issued — one bill per (meter_id, year, month) group, so `count('*')` returns the number of monthly billing rows the pipeline will produce.",
              "Days the meter reported a reading — input is one row per daily reading; after grouping by (meter_id, customer_type, year, month), `count('*')` counts daily reading rows per meter-month.",
              "Total daily_kw across the month — `count('*')` aggregates the value column when paired with another sum aggregate inside the same `.agg()` call, producing the monthly kW total.",
              "Distinct days with non-null readings — `count('*')` automatically deduplicates by date and skips rows where the value column is null, equivalent to `countDistinct(date)` filtered on non-null daily_kw."
            ],
            "correctAnswer": "Days the meter reported a reading — input is one row per daily reading; after grouping by (meter_id, customer_type, year, month), `count('*')` counts daily reading rows per meter-month.",
            "rationale": "The input to the groupBy is the meters table — one row per daily reading. After grouping by (meter_id, customer_type, year, month), `count('*')` counts rows in each group, which is the number of daily readings the meter produced that month. The alias 'days_reported' is correctly named because each input row is one day's reading per meter. If the source had hourly granularity, this would count hours, not days. The most common Junior confusion is reading `count('*')` literally; what it counts is determined by the input row granularity, not by the alias.",
            "distractorRationale": "Option A: Confuses output rows with input rows; the result has one row per group, but `count('*')` counts INPUT rows per group.\n\nOption C: Confuses count with sum; `count('*')` counts rows, not values, regardless of which other aggregates are in the same `.agg()`.\n\nOption D: Imagines `count('*')` automatically deduplicates and filters nulls; it does not — `count('*')` counts every row in the group unconditionally."
          },
          {
            "id": "JA6-02-Q3",
            "type": "single_select",
            "label": "Q3 — If a meter_id exists in the meters table but has zero readings in a given month, does it appear in the `monthly_per_meter` output for that month?",
            "options": [
              "Yes — empty groups still produce a row with sum=0 and count=0; PySpark scaffolds zero-rows for any expected groupBy combination based on column distinct values.",
              "No — `groupBy` yields no row for groups absent from the input; a meter with zero readings has no input rows for that month, so no (meter_id, year, month) group exists and no output row is produced.",
              "Yes — but only when `how='outer'` is set on the tariff join, which back-fills the missing meter-month combinations with default values for downstream billing.",
              "Only if the tariff join is changed to `how='right'` — that pulls in any tariff-eligible customer_type and produces zero-reading rows for the meters tied to it."
            ],
            "correctAnswer": "No — `groupBy` yields no row for groups absent from the input; a meter with zero readings has no input rows for that month, so no (meter_id, year, month) group exists and no output row is produced.",
            "rationale": "`groupBy` produces rows only for groups present in the input. A meter that reported zero readings in a month has no input rows for that month — so no (meter_id, year, month) group is formed, and no output row appears. To produce a 'zero-reading' bill row, the engineer must scaffold a complete (meter × month) cross-join first, then left-join actual readings onto it. This is a common billing pattern, absent here — the pipeline SILENTLY omits zero-reading meters from the bill. The most common Junior confusion is expecting groupBy to fill in expected combinations; it only emits combinations actually present.",
            "distractorRationale": "Option A: Imagines groupBy scaffolds zero-rows for missing combinations; it only emits combinations actually present in the input.\n\nOption C: Conflates the tariff join (which is downstream of the groupBy) with the missing-row behaviour; the join cannot create rows the groupBy did not produce.\n\nOption D: Same conflation as C; right join would not back-fill meter-month combinations either, since the groupBy result is what feeds the join."
          },
          {
            "id": "JA6-02-Q4",
            "type": "single_select",
            "label": "Q4 — What is the role of the `coalesce` calls on line 31, and what happens if you remove them?",
            "options": [
              "They prevent division by zero in the multiplication; without them, multiplying by a zero tariff would raise ArithmeticException for currency-typed columns at runtime.",
              "They null-guard `total_kw` and `eur_per_kw` before the multiplication; without them, a null on either side propagates to `bill_eur = null` for that row, which a downstream sum then drops.",
              "They are redundant — Spark already converts null arithmetic to zero automatically when building bill rows, so the coalesce calls add code without changing behaviour.",
              "They convert string-typed nulls to numeric zeros for Power BI compatibility; without them, the BI layer renders the null as the string 'null' and breaks aggregations downstream."
            ],
            "correctAnswer": "They null-guard `total_kw` and `eur_per_kw` before the multiplication; without them, a null on either side propagates to `bill_eur = null` for that row, which a downstream sum then drops.",
            "rationale": "The `coalesce` calls protect against null propagation through the multiplication. `total_kw` is null if every daily_kw in a group is null (sum returns null only then). More commonly, `eur_per_kw` is null when a customer_type appears in meters but not in the tariff lookup (a new customer_type added upstream before tariffs are updated). Null × anything = null. Removing the coalesce makes those rows produce `bill_eur = null`, which the line-41 sum silently drops as a non-contribution. The most common Junior confusion is expecting Spark to treat null as zero in arithmetic; null propagates through every operator unless explicitly guarded.",
            "distractorRationale": "Option A: Confuses null-guarding with division-by-zero; multiplication of a number by zero is well-defined, the issue is null propagation.\n\nOption C: Reverses Spark's actual null behaviour; null is propagated through arithmetic, not auto-converted to zero.\n\nOption D: Misframes null types; PySpark's null is a typed null, not a string, and the BI compatibility framing is unrelated."
          },
          {
            "id": "JA6-02-Q5",
            "type": "single_select",
            "label": "Q5 — Which line shows a pattern that should make a senior reviewer pause?",
            "options": [
              "Line 18 — `groupBy('meter_id', 'customer_type', 'year', 'month')` over-specifies the keys; customer_type is a function of meter_id and including it adds work without changing row count.",
              "Lines 36-37 — `meters.select('meter_id', 'region').distinct()` followed by a join recreates information already present in the original meters table; carrying region through the line-18 groupBy would skip both operations.",
              "Line 26 — the left join on customer_type is the wrong join type for billing; an inner join would be safer because meters without a tariff entry should be excluded.",
              "Line 31 — the bill_eur multiplication should use `*` on Column objects rather than wrapping in coalesce, which obscures the arithmetic from the optimizer."
            ],
            "correctAnswer": "Lines 36-37 — `meters.select('meter_id', 'region').distinct()` followed by a join recreates information already present in the original meters table; carrying region through the line-18 groupBy would skip both operations.",
            "rationale": "Lines 36-37 do extra work that the line-18 groupBy could absorb. The meters DataFrame already carries a `region` column; the cleaner pattern is to add region to the line-18 groupBy keys: `groupBy('meter_id', 'customer_type', 'region', 'year', 'month')`. Region is a function of meter_id (each meter belongs to one region), so adding it does not change row count, but carries region through the aggregation for free — eliminating both the distinct (a shuffle) and the join (another shuffle). The most common Junior confusion is treating the lookup-table-rebuild pattern as natural; senior reviewers spot it as a pair of avoidable shuffles.",
            "distractorRationale": "Option A: Calls customer_type unnecessary; it is a real billing key (different tariffs apply), so it must be in the groupBy.\n\nOption C: Reverses the join semantics; left preserves meters whose customer_type is missing from tariffs, which is what billing audits need.\n\nOption D: Misframes coalesce; the multiplication is `*`, and coalesce wraps the operands, not the operation."
          }
        ]
      }
    },
    {
      "id": "module-JA6-task-03",
      "title": "The Slow Pipeline That Looks Right",
      "type": "refactor_reading",
      "estimatedMinutes": 18,
      "description": {
        "context": "Three patterns combine to slow a correct pipeline: (1) running SEPARATE aggregations and joining the results — each separate aggregation is one shuffle, and the join is another, when one stacked `.agg()` would do them all in one shuffle; (2) intermediate `.count()` or `.show()` calls — each is an ACTION that triggers full re-execution of the upstream plan from scratch (Spark does not auto-cache between actions); (3) filters AFTER the groupBy on a column that exists pre-groupBy — those rows wasted work being shuffled and aggregated only to be discarded. Recognising these is a Junior-level reading exercise; the code looks correct because it IS correct, only slow.",
        "task": "Read the pipeline carefully (line numbers shown), then answer the questions below."
      },
      "evidence": [
        {
          "type": "code_block",
          "label": "regional_summary.py",
          "language": "python",
          "content": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, sum, avg, count, when\n\nspark = SparkSession.builder.appName(\"regional_summary\").getOrCreate()\n\nmeters = spark.read.parquet(\"silver/meters/\")\n\n# Step 1: per-region totals\nregional_totals = (meters\n    .groupBy(\"region\")\n    .agg(sum(\"daily_kw\").alias(\"total_kw\")))\n\n# Step 2: per-region averages (separate aggregation pass)\nregional_avgs = (meters\n    .groupBy(\"region\")\n    .agg(avg(\"daily_kw\").alias(\"avg_kw\")))\n\n# Sanity check that nothing got lost\nprint(f\"Total rows in totals: {regional_totals.count()}\")\nprint(f\"Total rows in avgs:   {regional_avgs.count()}\")\n\n# Combine the two\nsummary = regional_totals.join(regional_avgs, on=\"region\", how=\"inner\")\n\n# Step 3: enrich with customer-type breakdown (used by the dashboard slicer)\nby_cust_type = (meters\n    .groupBy(\"region\", \"customer_type\")\n    .agg(sum(\"daily_kw\").alias(\"ct_total_kw\")))\n\nsummary_full = summary.join(by_cust_type, on=\"region\", how=\"inner\")\n\n# Drop INDUSTRIAL — finance excluded from this report\nsummary_full = summary_full.filter(col(\"customer_type\") != \"INDUSTRIAL\")\n\n# Final write\nsummary_full.write.mode(\"overwrite\").parquet(\"gold/regional_summary/\")\n\nprint(f\"Final row count: {summary_full.count()}\")\n"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA6-03-Q1",
            "type": "single_select",
            "label": "Q1 — Which lines are responsible for the runtime increase?",
            "options": [
              "Lines 8-11 — the read and early filter sequence is narrow but produces a wide intermediate that bloats memory before the aggregations even run.",
              "Lines 14-16 + 19-20 — two separate aggregations that should be one stacked `.agg()`, plus intermediate `.count()` calls that each re-execute the upstream plan from scratch.",
              "Lines 28-30 — the customer_type breakdown is the dominant cost because it runs a full broadcast join against the substations table on every batch.",
              "Line 38 — the final partitioned write fragments output into many small files and the write coordination dominates the runtime over the actual aggregation work."
            ],
            "correctAnswer": "Lines 14-16 + 19-20 — two separate aggregations that should be one stacked `.agg()`, plus intermediate `.count()` calls that each re-execute the upstream plan from scratch.",
            "rationale": "Three patterns combine for the 4× slowdown. (1) Separate sum (lines 9-11) and avg (lines 14-16) aggregations cause TWO shuffles instead of one, plus a join to reunite them. (2) Intermediate `.count()` calls on lines 19-20 each execute the plan from scratch — Spark does not auto-cache between actions, so the meters read happens THREE times across the pipeline. (3) The filter on line 33 applied AFTER the customer_type breakdown means INDUSTRIAL rows participate in the shuffle and aggregation only to be discarded. The most common Junior confusion is treating intermediate `.count()` prints as 'just printing'; each one is a full pipeline re-execution.",
            "distractorRationale": "Option A: Targets narrow read+filter; these run once per action and are not the dominant cost.\n\nOption C: Misframes the customer_type breakdown as a join; it is a groupBy, not a join, and it operates on the meters table directly without a substations join.\n\nOption D: Targets the final write; partitioned writes are not the dominant cost at this scale, the upstream redundant work is."
          },
          {
            "id": "JA6-03-Q2",
            "type": "single_select",
            "label": "Q2 — Of the inefficiencies identified, which one would a junior engineer most likely KEEP on a first pass at refactoring?",
            "options": [
              "The separate avg aggregation (lines 14-16) — Junior engineers see two named DataFrames as a clarity win and refactoring them into a single `.agg()` looks like premature optimization at first glance.",
              "The intermediate `.count()` prints (lines 19-20) — they look like helpful sanity checks and the cost is invisible at the source-code level; a Junior reads them as 'just printing a number' and leaves them in.",
              "The post-groupBy filter on customer_type (line 33) — the comment makes the filter look intentional and Junior engineers do not yet see filter-pushdown failures as fixable.",
              "All three are equally likely to be kept — Junior engineers refactor on PR feedback only, and none of these patterns reads as a bug to a fresh reviewer."
            ],
            "correctAnswer": "The intermediate `.count()` prints (lines 19-20) — they look like helpful sanity checks and the cost is invisible at the source-code level; a Junior reads them as 'just printing a number' and leaves them in.",
            "rationale": "The intermediate `.count()` prints are the hardest pattern to refactor away. They look like helpful debug output that the original engineer genuinely wanted (to verify row counts match), and the cost is INVISIBLE at the source level — a Junior reads them as 'just printing a number' and does not realise each one re-executes the upstream plan. The separate-aggregation pattern is more visible after PS6 lesson 4. The filter-after-groupBy is what a Junior with PS6 lesson 10 freshly read SHOULD spot. The most common Junior confusion is conflating 'looks innocent' with 'cheap'.",
            "distractorRationale": "Option A: Treats clarity as the primary tradeoff; Junior engineers reading PS6 lesson 4 should already see the duplicated shuffle.\n\nOption C: Treats the filter as intentional; the post-groupBy filter on a column existing pre-groupBy is a textbook PS6 lesson 10 anti-pattern.\n\nOption D: Treats all three as equally invisible; the intermediate count is the only one whose cost is invisible AT THE SOURCE — the others are visible as duplicated work after one PS6 read."
          },
          {
            "id": "JA6-03-Q3",
            "type": "single_select",
            "label": "Q3 — What is the production cost of leaving the intermediate `.count()` calls (lines 19-20) in the pipeline?",
            "options": [
              "Negligible — Spark caches plan output between actions automatically, so subsequent actions reuse the materialized result and the count is essentially free.",
              "Each `.count()` is an action that triggers full re-execution of the upstream plan; Spark does not auto-cache between actions, so the meters read and aggregations run again from scratch for each count.",
              "The `.count()` calls force a Spark UI refresh that contends with the executor heartbeat thread; the cost is dominated by UI coordination, not by re-execution of the plan.",
              "The `print()` statements log to stdout each call; the IO buffering on the driver is the cost, and removing the prints (but keeping the counts) eliminates the slowdown."
            ],
            "correctAnswer": "Each `.count()` is an action that triggers full re-execution of the upstream plan; Spark does not auto-cache between actions, so the meters read and aggregations run again from scratch for each count.",
            "rationale": "Each `.count()` is an ACTION. Spark does not cache intermediate DataFrames automatically — `regional_totals.count()` executes the plan from the meters read forward, computes the sum aggregate, counts. Then `regional_avgs.count()` does it again from the meters read forward, computes avg, counts. Then the final `summary_full.count()` executes the entire pipeline from scratch. The most common Junior confusion is assuming Spark caches output of every action; without explicit `.cache()`, each action re-runs the lineage from the source.",
            "distractorRationale": "Option A: Asserts auto-caching; Spark requires explicit `.cache()` or `.persist()` for action-to-action reuse.\n\nOption C: Invents UI coordination cost; the Spark UI is updated lazily and does not block executor work.\n\nOption D: Targets `print` IO; print of a single integer is microseconds; the cost is the upstream re-execution before count produces the integer."
          },
          {
            "id": "JA6-03-Q4",
            "type": "single_select",
            "label": "Q4 — If you could only make one fix to this pipeline, which would have the LARGEST runtime impact?",
            "options": [
              "Remove lines 19-20 — eliminating the two intermediate `.count()` prints removes two full pipeline re-executions, which is the single biggest source of duplicated work.",
              "Combine the sum and avg into one `.agg()` call — this saves one shuffle and a join, which is the dominant cost in any pipeline that touches a 500K-row table.",
              "Move the filter on line 33 before the customer_type groupBy — this saves ~25% of the aggregation cost by excluding INDUSTRIAL rows from the shuffle.",
              "Cache the meters DataFrame after reading it — caching at the source eliminates the redundant reads and is the simplest one-line fix that addresses every other inefficiency."
            ],
            "correctAnswer": "Remove lines 19-20 — eliminating the two intermediate `.count()` prints removes two full pipeline re-executions, which is the single biggest source of duplicated work.",
            "rationale": "Removing the intermediate `.count()` prints is the largest single win — it eliminates TWO extra full pipeline re-executions plus the work that built each DataFrame. The other fixes are real but smaller: combining sum/avg saves one shuffle; pushing the filter saves ~25% of one aggregation; caching is a band-aid that does not address the underlying redundancy and adds memory pressure. Production rule: in dev notebooks `.count()` is fine; in production it is a full re-execution of everything upstream. The most common Junior confusion is comparing fixes by 'how clean the diff looks' rather than by the cost they remove.",
            "distractorRationale": "Option B: Real win but smaller than removing two upstream re-executions; one shuffle saved vs two pipeline re-runs.\n\nOption C: Real win but smaller; pushing the filter saves work in ONE aggregation, while removing counts saves TWO full pipeline runs.\n\nOption D: Caching at source is a band-aid; it reduces re-read cost but the count() actions still re-execute the aggregations downstream of the cache."
          }
        ]
      }
    },
    {
      "id": "module-JA6-task-04",
      "title": "The Pipeline With the Subtle Bug",
      "type": "refactor_reading",
      "estimatedMinutes": 22,
      "description": {
        "context": "Four canonical Junior-level aggregation bugs hide in this pipeline. (1) Defensive `.filter(col.isNotNull())` silently drops real data (JA4.4). (2) An unaliased `.agg(sum('daily_kw'))` produces column `sum(daily_kw)` and breaks any downstream reference to `daily_kw` (JA1.3). (3) `count('*')` counts all rows including those with null daily_kw, mismatching the alias 'readings' (JA4.1). (4) Averaging an already-averaged column (`avg(avg_kw)`) gives the WEIGHTED-WRONG answer when day-counts vary across groups (JA4.3). Each bug runs cleanly, produces plausible numbers, and escapes review unless the reader pattern-matches against known anti-patterns.",
        "task": "Read the pipeline carefully (line numbers shown), then answer all questions below."
      },
      "evidence": [
        {
          "type": "code_block",
          "label": "ops_dashboard_feed.py",
          "language": "python",
          "content": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, sum, count, avg, when\n\nspark = SparkSession.builder.appName(\"ops_dashboard_feed\").getOrCreate()\n\n# Read the cleansed meter readings (Silver)\nmeters = spark.read.parquet(\"silver/meters/\")\n\n# Defensive: drop any row with null region (added after the dashboard\n# crashed on a null value in February)\nmeters = meters.filter(col(\"region\").isNotNull())\n\n# Daily-region-customer_type aggregation for the operations dashboard.\n# Spec: dashboard shows daily total kW, daily reading count, and daily avg\n# per region per customer_type per date.\nops_feed = (meters\n    .groupBy(\"region\", \"customer_type\", \"date\")\n    .agg(\n        sum(\"daily_kw\"),\n        count(\"*\").alias(\"readings\"),\n        avg(\"daily_kw\").alias(\"avg_kw\")\n    ))\n\n# Compute a \"high-volume flag\" for groups whose total exceeds 5000 kW\n# (the dashboard highlights these in red)\nops_feed = ops_feed.withColumn(\n    \"high_volume\",\n    when(col(\"daily_kw\") > 5000, \"YES\").otherwise(\"NO\")\n)\n\n# Filter to only the most recent month for the dashboard refresh\n# (the dashboard shows a 30-day rolling window)\nops_feed = ops_feed.filter(col(\"date\") >= \"2026-03-01\")\n\n# Sanity-check row count\nprint(f\"Daily-region-cust_type rows: {ops_feed.count()}\")\n\n# Roll up to monthly level for the trend panel\ntrend = (ops_feed\n    .groupBy(\"region\", \"customer_type\")\n    .agg(\n        sum(\"readings\").alias(\"month_readings\"),\n        avg(\"avg_kw\").alias(\"month_avg_kw\")\n    ))\n\n# Write both feeds\nops_feed.write.mode(\"overwrite\").parquet(\"gold/ops_feed_daily/\")\ntrend.write.mode(\"overwrite\").parquet(\"gold/ops_feed_trend/\")\n"
        }
      ],
      "template": {
        "fields": [
          {
            "id": "JA6-04-Q1",
            "type": "single_select",
            "label": "Q1 — Bug #1 (null handling). Which line silently drops data, and what is the production consequence?",
            "options": [
              "Line 11 — `meters.filter(col('region').isNotNull())` silently drops the ~240 meters with null region; their real daily_kw never enters the dashboard, which under-reports by that amount.",
              "Line 7 — reading from `silver/meters/` already filters out null-region rows in the silver layer, so the line-11 filter is redundant but does not itself drop additional rows.",
              "Line 22 — `count('*')` is the data-drop because it skips rows with null region; switching to `count(col('region'))` would catch them.",
              "Line 36 — `ops_feed.count()` materializes the result and triggers a Spark optimization that drops null-region rows from the cache."
            ],
            "correctAnswer": "Line 11 — `meters.filter(col('region').isNotNull())` silently drops the ~240 meters with null region; their real daily_kw never enters the dashboard, which under-reports by that amount.",
            "rationale": "Line 11's defensive filter looks reasonable and prevents the dashboard crash that motivated it. But it also silently drops every meter whose region is null — about 240 meters at NordGrid scale. Those readings represent real consumption from real customers; their kW is real, only the region tag is missing. The dashboard under-reports by exactly that consumption. Fix: handle null-region rows explicitly via quarantine path, 'UNKNOWN' label, or surface a count metric. The most common Junior confusion is treating defensive null-filters as harmless hygiene; they are silent data drops with downstream consequences (JA4.4).",
            "distractorRationale": "Option B: Imagines silver-layer pre-filtering; the snippet shows the filter explicitly on line 11, that is what drops the rows.\n\nOption C: Misframes count as data-drop; count is a measurement, not a filter; it does not remove rows from the output.\n\nOption D: Invents a Spark cache-drop optimization; `.count()` materializes a count, it does not transform the upstream DataFrame."
          },
          {
            "id": "JA6-04-Q2",
            "type": "single_select",
            "label": "Q2 — Bug #2 (aliasing). Which line has a missing alias, and what breaks downstream?",
            "options": [
              "Line 19 — `sum('daily_kw')` is missing `.alias('total_kw')`; after `.agg()` the column is named `sum(daily_kw)`, so `col('daily_kw')` in the line-28 `withColumn` cannot be resolved and Spark raises AnalysisException.",
              "Line 21 — `count('*').alias('readings')` mis-aliases an aggregate that should be tied to a specific column; without a column reference the alias does not bind to the source.",
              "Line 22 — `avg('daily_kw').alias('avg_kw')` shadows the line-19 sum aggregate because both reference daily_kw; Spark applies the avg alias to both aggregates and breaks downstream.",
              "Line 41 — `sum('readings')` rolls up a column already collapsed by line-21 count; the missing alias on the rollup makes the trend table lose its readings column."
            ],
            "correctAnswer": "Line 19 — `sum('daily_kw')` is missing `.alias('total_kw')`; after `.agg()` the column is named `sum(daily_kw)`, so `col('daily_kw')` in the line-28 `withColumn` cannot be resolved and Spark raises AnalysisException.",
            "rationale": "Line 19 omits `.alias('total_kw')`. After the aggregation, the column is named `sum(daily_kw)` — NOT `daily_kw`. The `withColumn` on line 28 then references `col('daily_kw')`, which no longer exists in the schema, and PySpark raises AnalysisException at plan resolution. If the pipeline were wrapped in a try/except (JA3.4 pattern), the error would be swallowed and high_volume would be misclassified on every row. Fix: `.agg(sum('daily_kw').alias('total_kw'), ...)` and `col('total_kw') > 5000` in withColumn. The most common Junior confusion is expecting the source column to remain after aggregation; it does not.",
            "distractorRationale": "Option B: `count('*').alias('readings')` is structurally fine (alias on a count expression); the bug is its semantics, not its alias.\n\nOption C: `avg('daily_kw').alias('avg_kw')` does not shadow the sum; PySpark binds aliases per-expression.\n\nOption D: Line 41 reads `count('readings')` correctly; the issue is upstream, not at the rollup."
          },
          {
            "id": "JA6-04-Q3",
            "type": "single_select",
            "label": "Q3 — Bug #3 (count semantics). Which line uses `count()` in a way that does not match its alias?",
            "options": [
              "Line 21 — `count('*').alias('readings')` counts ALL rows including null-daily_kw rows (sensor outages, ingestion glitches), so the alias 'readings' is inflated by rows that produced no actual measurement.",
              "Line 41 — `sum('readings')` should be `count('readings')` because the rollup must count not sum; the alias on a sum mismatches the consumer's expectation of a count.",
              "Line 22 — `avg('daily_kw')` returns the mean of non-null values, which the line-21 alias 'readings' is double-counting; this is the actual mismatch.",
              "Line 21 is correct as written — null-daily_kw rows are still 'readings' from the meter's perspective even when the measurement value is missing, so counting them is the intended behaviour."
            ],
            "correctAnswer": "Line 21 — `count('*').alias('readings')` counts ALL rows including null-daily_kw rows (sensor outages, ingestion glitches), so the alias 'readings' is inflated by rows that produced no actual measurement.",
            "rationale": "Line 21 uses `count('*')` aliased 'readings'. `count('*')` counts every row in each group regardless of whether daily_kw is null. If a meter reports a row with null daily_kw (sensor problem, outage, ingestion glitch), it is counted as a reading even though no measurement was produced. The dashboard's reading count is inflated by exactly the null-daily_kw row count. Fix: `count('daily_kw').alias('readings')` counts only rows that produced a non-null measurement. The most common Junior confusion is treating `count('*')` and `count('col')` as interchangeable; their null policies differ (JA4.1).",
            "distractorRationale": "Option B: Line 41 is `sum('readings')`, which is correct for rolling up an existing count column to a higher level.\n\nOption C: avg of daily_kw and the readings count are independent aggregates; one does not 'double-count' the other.\n\nOption D: Reframes the bug as a feature; from the dashboard consumer's perspective 'reading count' implies actual measurements, not row count."
          },
          {
            "id": "JA6-04-Q4",
            "type": "single_select",
            "label": "Q4 — Bug #4 (aggregation of an aggregate). Which line aggregates a derived metric in a way that produces a misleading number?",
            "options": [
              "Line 44 — `avg('avg_kw')` averages the per-day averages and gives every day equal weight; this is mathematically different from the true mean of all daily readings whenever day-counts vary across groups.",
              "Line 43 — `sum('readings').alias('month_readings')` rolls up an inflated count from the line-21 bug; this is the misleading number, not anything on line 44.",
              "Line 39 — the trend `groupBy('region', 'customer_type')` is at the wrong level; rolling up by region without date produces a meaningless aggregate.",
              "Line 18 — `groupBy('region', 'customer_type', 'date')` is too granular for the trend panel; the daily values cannot be meaningfully aggregated upward."
            ],
            "correctAnswer": "Line 44 — `avg('avg_kw')` averages the per-day averages and gives every day equal weight; this is mathematically different from the true mean of all daily readings whenever day-counts vary across groups.",
            "rationale": "Line 44 computes `avg('avg_kw')` — the average of per-day averages. This is the WEIGHTED-WRONG answer compared to the true mean of all daily readings. Each daily group contributes ONE avg_kw value to the rollup, and `avg()` weights every day equally regardless of how many readings the day represented. The right pattern is to compute weighted averages: `sum('total_kw') / sum('readings')` at rollup. The most common Junior confusion is treating averaging as commutative across aggregation levels; it is not, except in the rare case where every group has identical row counts (JA4.3).",
            "distractorRationale": "Option B: Line 43 inherits the line-21 bug but is just a sum rollup; the math is right, the inputs are wrong (which is bug #3).\n\nOption C: The trend rollup is at a sensible level (per region/customer_type across the month); the issue is the avg-of-avg, not the level.\n\nOption D: Line 18's daily granularity is appropriate for an ops feed; the trend on line 39 rolls it up correctly except for the avg-of-avg."
          },
          {
            "id": "JA6-04-Q5",
            "type": "single_select",
            "label": "Q5 — Of the four bugs, which is most likely to remain UNDETECTED in production for the longest?",
            "options": [
              "Bug #1 (null region drop) — the regulatory team eventually reconciles total kW and notices the ~2-3% gap, surfacing the bug within a quarter.",
              "Bug #2 (missing alias) — Spark raises AnalysisException on the first run, so the bug surfaces immediately and never reaches production.",
              "Bug #3 (count vs count(col)) — operations notices that meter counts seem high when comparing against the SCADA event counter, surfacing within weeks.",
              "Bug #4 (avg of averages) — the dashboard renders smoothly, the math is invisible, nobody reconciles the trend independently, so the wrong values go unflagged for months or longer."
            ],
            "correctAnswer": "Bug #4 (avg of averages) — the dashboard renders smoothly, the math is invisible, nobody reconciles the trend independently, so the wrong values go unflagged for months or longer.",
            "rationale": "Bug #4 is the most pernicious. The dashboard renders without errors. The trend panel shows a smooth line. Nobody verifies the math because nobody recomputes from source. Operations sees the trend, notices that 'average monthly consumption' moves up or down, and ACTS on it. Values are wrong by an amount that depends on day-to-day variation in reading counts — sometimes a few percent, sometimes much more — and the wrongness is invisible to anyone not running their own reconciliation. The most common Junior confusion is conflating 'looks plausible' with 'is correct'; aggregation-of-aggregation bugs produce plausible numbers that no consumer will independently reconcile.",
            "distractorRationale": "Option A: Regulatory reconciliation does surface bug #1 eventually, but the problem is well-known and reconciliation runs quarterly, so it surfaces in months not years.\n\nOption B: Bug #2 raises AnalysisException at runtime; if uncaught it would surface on the first dashboard run and crash before reaching production.\n\nOption C: SCADA reconciliation does not run automatically against this pipeline; bug #3 inflates by null-daily_kw count which is small (5-10%) and harder to attribute, but still more visible than the avg-of-avg bug."
          }
        ]
      }
    }
  ]
}