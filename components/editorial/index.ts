/**
 * Editorial / data-tech design system primitives.
 *
 * These components implement the new visual identity (Stripe Press × Bloomberg
 * Terminal × Substack — serif headlines, ruled lines, sharp corners, single
 * vermillion accent on cream paper). They are introduced alongside the
 * existing Apple-influenced UI; surfaces migrate piecemeal as their PRs land.
 *
 * Tokens: see `tailwind.config.ts` (`ink.*`, `paper.*`, `rule.*`, `vermillion`,
 * `font-editorial`, `font-editorial-sans`, `font-editorial-mono`).
 */

export { Kicker } from './Kicker';
export { Rule } from './Rule';
